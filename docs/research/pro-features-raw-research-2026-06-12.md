{
  "summary": "Research ANTARES pro capabilities, difference-imaging/forced-photometry use cases, pro UX redesign, accurate observational features, browser-direct data sources + FITS handling; synthesize into a redesign spec, FITS plan, install help, and a pro-feature roadmap",
  "agentCount": 7,
  "logs": [
    "research complete: 6/6 agents returned",
    "[synthesize] failed: You've hit your session limit · resets 7:40pm (Asia/Calcutta)"
  ],
  "result": {
    "research": [
      {
        "summary": "ANTARES (NOIRLab CSDC) is a full-stream LSST/Rubin alert broker that already ingests both live ZTF and live Rubin/LSST alerts (confirmed: a Rubin locus dated 2026 returned from the production API). Everything below is verified live against the production JSON:API at https://api.antares.noirlab.edu/v1 unless noted. KEY ACCESS REALITY: the REST API is fully browser-usable — CORS is wide open (Access-Control-Allow-Origin reflects the requesting Origin, Access-Control-Allow-Credentials: true), so a pro web app can call loci/cone/tags/catalogs/alerts/lightcurve directly from the browser with no proxy. The website pages (antares.noirlab.edu/loci, /filters) are a client-rendered React app that returns only the bare word \"ANTARES\" to server-side fetchers, so scrape the API, not the HTML. There is NO public REST endpoint for watchlists, saved searches, subscriptions, or notifications — those live only in the authenticated web portal and via Slack/Kafka (out-of-band), and user-defined filters are submitted as Python via the DevKit, not via API. CONCRETE LAYOUT RECOMMENDATION for a pro app: (1) an object page keyed by ANTARES locus_id / ztf_object_id / lsst diaObjectId showing the CSV lightcurve (with non-detection upper-limit rows and host-corrected mags), the science/template/difference cutout triptych (direct googleapis PNG URLs, no auth), the full alert-property table (ZTF rb/drb + sgscore/distpsnr, or Rubin reliability/psfFlux/extendedness), and the catalog cross-match panel (Gaia DR3, PS1, TNS, etc.); (2) a \"stream/tag explorer\" driven by the 37-tag taxonomy (nuclear_transient, extragalactic, high_snr, anomaly, SSO, superphot_plus classifications, Rubin young_rubin_transients/SN_candies); (3) a cone/ElasticSearch search panel. The single highest-value differentiator is that ANTARES is one of the few brokers already serving the FULL Rubin diaSource/diaObject schema with a real/bogus 'reliability' score in production.",
        "items": [
          {
            "title": "Loci listing & search endpoint (GET /v1/loci) — JSON:API shape, pagination, sort",
            "detail": "Verified live. GET https://api.antares.noirlab.edu/v1/loci returns JSON:API: top-level {data:[...], links:{self,next,prev}, meta:{count}}. Each item: {type:'locus_listing', id:'ANT2021y65ce', attributes:{ra,dec,htm16,tags:[],catalogs:[],properties:{...}}, relationships:{alerts:{links:{related}}}, meta:{newest_thumbnails:[...]}}. Pagination via bracketed params page[limit] and page[offset] (e.g. ?page[limit]=10&page[offset]=10; client default limit 100). Sort via sort=-properties.newest_alert_observation_time (minus = descending; default order is most-recently-updated first). meta.count caps at 10000 (ES result window limit) — use scroll/refine for deeper. locus 'properties' on the listing: ztf_object_id, ztf_ssnamenr, num_alerts, num_mag_values, oldest/newest/brightest_alert_id, oldest/newest/brightest_alert_magnitude, oldest/newest/brightest_alert_observation_time (MJD), anomaly_type.",
            "dataOrApi": "GET /v1/loci (JSON:API). Params: page[limit], page[offset], sort, elasticsearch_query[locus_listing]. CORS: OPEN — Access-Control-Allow-Origin echoes Origin, credentials allowed; callable directly from browser fetch().",
            "proValue": "The primary discovery/list surface. Lets a pro app page through transients, sort by recency/brightness, and pivot to detail by locus_id or ztf_object_id without server-side glue.",
            "accuracy": "meta.count hard-caps at 10000 even when more match; do not present it as a true total. Times are MJD (UTC-based ant_mjd); brightness is magnitude (lower=brighter).",
            "effort": "Low — plain REST+JSON, fetch directly in browser.",
            "source": "https://api.antares.noirlab.edu/v1/loci ; https://nsf-noirlab.gitlab.io/csdc/antares/client/api.html"
          },
          {
            "title": "Cone search + ElasticSearch DSL query (elasticsearch_query[locus_listing])",
            "detail": "Cone search and arbitrary filtering are both done by passing a URL-encoded ElasticSearch query under the param elasticsearch_query[locus_listing] (verified working live: filtering by {term:{tags:'nuclear_transient'}} and {term:{tags:'lsst_scimma_quality_transient'}} returned matching loci). Query must be a dict with a single top-level 'query' key. Supports bool with must/should/must_not/filter, range (gt/gte/lt/lte), term/terms, and sky_distance (cone) on htm16/center. Searchable fields include: ra, dec, tags, catalogs, locus_id, and properties.* (properties.num_mag_values, properties.num_alerts, properties.brightest_alert_magnitude, properties.newest_alert_observation_time, properties.anomaly_score, etc.). The Python client wraps this as antares_client.search.search(query) and cone_search(center:SkyCoord, radius:Angle). Locus→Alert association radius is 1 arcsec. The website's own cone search URL encodes a sky_distance filter with htm16 center + distance in degrees.",
            "dataOrApi": "GET /v1/loci?elasticsearch_query[locus_listing]=<urlencoded ES JSON>&sort=...&page[limit]=... ; cone_search via sky_distance filter. CORS open (browser-callable).",
            "proValue": "Power-user search: arbitrary boolean/range queries over physical properties and stream tags, plus positional cone search for cross-identification and follow-up triage.",
            "accuracy": "ES query must have exactly one top-level 'query' key or it errors. Cone radius expressed in degrees inside the htm16 sky_distance field (1 arcsec = 0.0002778 deg). 1-arcsec locus association means blended/nearby sources can merge.",
            "effort": "Medium — must build/encode ES DSL; cone search needs HTM/coordinate handling (easier via the Python client).",
            "source": "https://nsf-noirlab.gitlab.io/csdc/antares/client/tutorial/searching.html ; https://api.antares.noirlab.edu/v1/loci"
          },
          {
            "title": "Direct lookups by survey ID (get_by_id / ztf / lsst diaObject / lsst ssObject)",
            "detail": "GET /v1/loci/{locus_id} returns the full single locus with attributes: ra, dec, tags, catalogs, properties, grav_wave_events, catalog_objects, and lightcurve (CSV string). Client lookup helpers (each hits the API): get_by_id('ANT2020j7wo4'), get_by_ztf_object_id('ZTF20aafqubg'), get_by_lsst_dia_object_id('169342393603063964'), get_by_lsst_ss_object_id(...) for Solar System objects, get_random_locus_id()/get_random_locus_ids(n).",
            "dataOrApi": "GET /v1/loci/{id} (full locus incl. lightcurve CSV + catalog_objects). CORS open.",
            "proValue": "Lets the app deep-link an object page from any survey identifier (ANTARES, ZTF, or Rubin diaObject/ssObject), enabling cross-survey object resolution.",
            "accuracy": "catalog_objects on the locus JSON can come back empty even when catalogs[] is populated — fetch matches via the catalog-matches sub-resource (see catalog item) for full data.",
            "effort": "Low.",
            "source": "https://nsf-noirlab.gitlab.io/csdc/antares/client/api.html"
          },
          {
            "title": "TAGS / stream taxonomy — GET /v1/tags (37 tags, live, with descriptions)",
            "detail": "GET /v1/tags?page[limit]=100 → meta.count=37, each {id, attributes:{description}}. The full live list with meanings: high_snr (alerts with high SNR); nuclear_transient (alert within <0.6 arcsec of a ZTF-reference source center — TDE/AGN/central SNe); extragalactic (within 1 arcsec of an extended/2MASS-XSC source); young_extragalactic_candidate (wide-net young extragalactic, NO catalog xmatch, galactic-plane & stars removed); high_amplitude_transient_candidate; high_amplitude_variable_star_candidate; high_flux_ratio_wrt_nn (big flux change vs nearest neighbor <1 arcsec); recent_reddening (g−r slope reddening in 14d); dimmers (persistently dimming); blue_transient / ECL_blue_transient_candidate / nova_test (blue/colour-evolution transients); dwarf_nova_outburst; in_m31 (line of sight to M31); refitt_newsources_snrcut (new sources SNR>5 for REFITT follow-up network); desi_target (brightness range for DESI); sso_candidates (known SSO ZTF detections) and sso_confirmed (subset with <1 arcsec residual vs JPL Horizons); iso_forest_anomaly_detection & LAISS_RFC_AD_filter & anomaly_transient_dmdt_visited_v1 (ML anomaly/rare-transient detectors); lc_feature_extractor (78 LC features) & matheson_extreme_vpdf (extreme delta-mag) & lantern_xgboost_t2.0.7_c0.95 (LAGN/AGN candidate); superphot_plus_classified (ML SN photometric classification); imhb_candidate (IMBH accretion variability); soraisam_sublum (ILOT candidate); siena_mag_coord_cut / NUTTelA_TAO / matheson / test_dev_* (telescope/site-specific & dev tags); plus RUBIN/LSST-specific tags: SN_candies (SN candidate in Rubin stream), young_rubin_transients, lsst_scimma_quality_transient (reliability>=0.6, no quality flags, not known SSO — feeds SCiMMA), uniform_random_sample (1% random LSST alerts). Tags are queryable as term filters in ES queries and define Kafka output streams (union/intersection of tags).",
            "dataOrApi": "GET /v1/tags (list + descriptions). Filter loci by tag via ES term on 'tags'. Kafka output streams keyed by tag are consumable but require ANTARES-issued credentials (not browser/REST). CORS open for the REST tags list.",
            "proValue": "This taxonomy IS the broker's scientific value-add. A pro app can offer curated science streams (TDE/nuclear, SLSN, anomaly, SSO, ML-classified SNe, Rubin young transients) as one-click feeds — the differentiator vs a raw survey firehose.",
            "accuracy": "Tags are filter-author-defined and vary in maturity (many test_dev_*/dev tags; random_tagger_filter is a test). 'extragalactic' uses catalog xmatch while 'young_extragalactic_candidate' deliberately does not — describe selection function honestly. nuclear_transient threshold is <0.6 arcsec.",
            "effort": "Low to list; Medium to wire each tag as a curated stream view. Real-time Kafka streams = High (credentialed, server-side).",
            "source": "https://api.antares.noirlab.edu/v1/tags ; https://iopscience.iop.org/article/10.3847/1538-3881/abd703"
          },
          {
            "title": "User-submitted filters & the live filter catalog — GET /v1/filters (16 live) + DevKit",
            "detail": "GET /v1/filters?page[limit]=60 → meta.count=16 active production filters, each {id, attributes:{name, description}}. Live examples: Nuclear Transient, Extragalactic (2MASS-XSC 1-arcsec), Highfluxchange, HighSNR, HighAmp, InM31 (2 deg of M31), Bad Detection (L1 bogus screen), Bad Seeing (L1 poor-seeing/elongated screen), Young and fast transient (Ho et al. 2020), SSO (JPL Horizons), VPDF, ECL_blue_transients_v1, superphot_plus_ztf_v1 & _v3 (de Soto et al. 2024 two-band ZTF photometric classifier), 'N Point and Point Span SLSN filter', RecentReddening. USER FILTER MODEL: filters are Python classes written against the ANTARES DevKit (subclass with a run(locus) that can set locus/alert properties, add tags, or halt the pipeline) and SUBMITTED to NOIRLab to run live on the real-time stream — there is no self-serve API for this; submission/review is via the DevKit + ANTARES team. L1 filters run first and gate the rest of the pipeline.",
            "dataOrApi": "GET /v1/filters (read-only catalog of active filters; CORS open). Authoring = antares_client/DevKit Python, submitted out-of-band (no REST write endpoint).",
            "proValue": "Lets the app surface provenance — which filter produced a tag — and explain selection. For sophisticated users, the app can document/link the DevKit path so they can deploy custom science filters whose tags then flow back through the same API.",
            "accuracy": "Only ~16 filters are 'active' via the catalog though 37 tags exist (some tags are legacy/dev/retired-filter or telescope-specific). Filter IDs are sparse/non-sequential. No programmatic filter submission.",
            "effort": "Low to display catalog. Custom-filter authoring is High (Python, review cycle).",
            "source": "https://api.antares.noirlab.edu/v1/filters ; https://nsf-noirlab.gitlab.io/csdc/antares/devkit/ ; https://nsf-noirlab.gitlab.io/csdc/antares/client/"
          },
          {
            "title": "Light curves — CSV string on the locus, exact columns incl. non-detections & host-corrected mags",
            "detail": "locus.attributes.lightcurve is a CSV STRING (verified). Exact header (in order): time,alert_id,ant_mjd,ant_survey,ant_ra,ant_dec,ant_passband,ant_mag,ant_magerr,ant_maglim,ant_mag_corrected,ant_magerr_corrected,ant_magulim_corrected,ant_magllim_corrected. Detections have ant_mag/ant_magerr populated; NON-DETECTIONS / UPPER LIMITS appear as rows with alert_id like 'ztf_upper_limit:...' where ant_mag/ant_magerr are blank and ant_maglim holds the 5-sigma limiting mag (ant_survey/ant_passband still set, e.g. passband '2'/'R'). The *_corrected columns are host-galaxy/reference-corrected magnitudes with asymmetric upper/lower limits (ant_magulim_corrected/ant_magllim_corrected). The Python client also exposes Locus.lightcurve as a pandas DataFrame and Locus.timeseries as an astropy TimeSeries.",
            "dataOrApi": "Inside GET /v1/loci/{id} → attributes.lightcurve (CSV text). Parse client-side. CORS open. Per-alert photometry also under /v1/loci/{id}/alerts (ant_mjd, ant_mag, ant_magerr, ant_maglim, ant_passband, ant_survey, ant_ra, ant_dec).",
            "proValue": "Ready-to-plot, survey-normalized photometry with explicit upper-limit handling and host-corrected magnitudes — directly drives the lightcurve chart on an object page without re-deriving ZTF/LSST flux→mag.",
            "accuracy": "It is CSV text, not JSON — must be parsed. Render non-detections as downward-triangle upper limits (blank mag + ant_maglim), never as points. ant_passband for ZTF is fid 1=g,2=r,3=i; corrected mags only meaningful where reference flux is reliable.",
            "effort": "Low–Medium (CSV parse + upper-limit rendering).",
            "source": "https://api.antares.noirlab.edu/v1/loci/ANT2022rn7i8w80zpbg ; https://nsf-noirlab.gitlab.io/csdc/antares/client/tutorial/searching.html"
          },
          {
            "title": "Catalog cross-matches — GET /v1/catalogs (25 catalogs) + per-locus /catalog-matches with full rows",
            "detail": "GET /v1/catalogs → meta.count=25 catalogs ANTARES cross-matches against (live names): gaia_dr3_gaia_source, gaia_dr3_variability, gaia_edr3_distances_bailer_jones (Bailer-Jones distances), tns_public_objects (Transient Name Server), PS1StarGalaxyCatalog, sdss_gals, sdss_stars, sdss_dr7, nyu_valueadded_gals, 2mass_psc, 2mass_xsc, allwise, veron_agn_qso, milliquas (Million Quasars), vii_274_bzcat5 (blazars), chandra_master_sources, xmm3_dr8, ned, RC3, csdr2, bright_guide_star_cat, asassn_variable_catalog_v2, vsx (Variable Star indeX), linear_ll, french_post_starburst_gals. Per-object matches: GET /v1/loci/{id}/catalog-matches returns type:'catalog_entry' with FULL catalog row properties (verified live: an AllWISE/2MASS match returned designation, decl, pmra/pmdec, ph_qual, j/h/k_m_2mass, glat/glon, ext_flg, htm20, cc_flags, etc.). The locus also carries a flat catalogs:[] list of which catalogs matched. Client helper: catalog_search(ra, dec).",
            "dataOrApi": "GET /v1/catalogs (list) and GET /v1/loci/{id}/catalog-matches (full matched rows, JSON:API, paginated). CORS open. catalog_search(ra,dec) in client.",
            "proValue": "Instant astrophysical context: is it a known Gaia star (variable? distance?), an SDSS/PS1 galaxy host, a known QSO/AGN/blazar (milliquas/veron/bzcat), an X-ray source (Chandra/XMM), a known variable (VSX/ASASSN), or already named on TNS — the core triage signal for classifying a transient.",
            "accuracy": "The flat locus.catalogs list and catalog_objects can lag/empty on the bare locus JSON; use the /catalog-matches sub-resource for actual rows. A 2MASS xsc match flags extended/host; Gaia star match argues Galactic. TNS match = already reported.",
            "effort": "Medium — render heterogeneous per-catalog schemas; each catalog has different columns.",
            "source": "https://api.antares.noirlab.edu/v1/catalogs ; https://api.antares.noirlab.edu/v1/loci/{id}/catalog-matches"
          },
          {
            "title": "Alert packet contents — full ZTF avro candidate fields surfaced (rb, drb, sgscore, distpsnr, PSF mags)",
            "detail": "GET /v1/loci/{id}/alerts returns per-alert {mjd, survey, processed_at, grav_wave_events, properties{...}}. For ZTF (survey='ztf') ANTARES surfaces essentially the entire ZTF alert candidate schema (verified live, 100+ fields), including: ant_mjd/ant_mag/ant_magerr/ant_maglim/ant_passband/ant_ra/ant_dec (normalized) PLUS raw ztf_*: ztf_magpsf, ztf_sigmapsf, ztf_magap, ztf_sigmagap, ztf_magapbig, ztf_magnr, ztf_sigmagnr, ztf_magzpsci, ztf_diffmaglim, ztf_fid, ztf_field, ztf_rcid, ztf_candid, ztf_isdiffpos, ztf_rb + ztf_rbversion (RealBogus), ztf_drb + ztf_drbversion (deep-learning RealBogus), ztf_scorr (detection SNR), ztf_chipsf, ztf_classtar, ztf_fwhm, ztf_elong, ztf_nbad, ztf_distnr (dist to nearest ref source), ztf_sgscore1/2/3 + ztf_distpsnr1/2/3 + ztf_objectidps1/2/3 + ztf_sgmag/srmag/simag/szmag1-3 (PS1 star/galaxy scores & photometry of 3 nearest neighbors), ztf_neargaia/ztf_maggaia (Gaia neighbor), ztf_ndethist/ztf_ncovhist/ztf_jdstarthist/ztf_jdendhist (detection history), ztf_ssnamenr/ztf_ssdistnr/ztf_ssmagnr (SSO match), ztf_tooflag, ztf_programid/ztf_programpi.",
            "dataOrApi": "GET /v1/loci/{id}/alerts (JSON:API, paginated page[limit]/page[offset]). CORS open.",
            "proValue": "Enables real/bogus quality gating (drb>0.9 etc.), star/galaxy separation (sgscore1, distpsnr1), and using sigmapsf/scorr for SNR — the exact fields pros use to filter junk and assess whether a detection is real and extragalactic.",
            "accuracy": "Prefer ztf_drb (deep-learning) over legacy ztf_rb for quality; always pair with the *version field. distnr/distpsnr in arcsec; sgscore1 near 0=galaxy-like,1=star-like. ant_mjd is the canonical time (UTC); ztf_jd is JD.",
            "effort": "Low to fetch; Medium to expose a meaningful subset (100+ fields — curate, don't dump).",
            "source": "https://api.antares.noirlab.edu/v1/loci/ANT2021y65ce/alerts"
          },
          {
            "title": "Rubin/LSST alert packet — full diaSource + diaObject schema with reliability (real/bogus), live in production",
            "detail": "VERIFIED LIVE: ANTARES is already ingesting Rubin/LSST alerts (a locus dated 2026 returned, survey='lsst'). For survey='lsst' alerts the properties expose the full Rubin DPDD schema (100+ fields): normalized ant_* plus lsst_diaSource_* and lsst_diaObject_*. diaSource fields include psfFlux/psfFluxErr, scienceFlux, templateFlux/templateFluxErr, apFlux/apFluxErr, snr, reliability (Rubin's real/bogus score — feeds lsst_scimma_quality_transient at >=0.6), band, midpointMjdTai, ra/dec(+Err+Cov), extendedness, ixx/iyy/ixy(+PSF) shape moments, isDipole/dipoleFitAttempted, isNegative, trail* (glint/streak/SSO trail params: trailLength/trailAngle/trailFlux), ssObjectId, an extensive pixelFlags_* set (bad/cr/edge/saturated/streak/injected/interpolated/nodata/suspect + *Center variants), psfFlux_flag*, shape_flag*, visit/detector/x/y. diaObject fields include diaObjectId, nDiaSources, per-band psfFluxMean/Min/Max/MeanErr/Ndata and scienceFluxMean for u,g,r,i,z,y, plus ra/dec(+Err+Cov) and validityStartMjdTai. Also lsst_observation_reason, lsst_target_name.",
            "dataOrApi": "GET /v1/loci/{id}/alerts where survey='lsst'; query Rubin objects by lsst_diaObject_diaObjectId or tags young_rubin_transients / SN_candies / lsst_scimma_quality_transient / uniform_random_sample. CORS open.",
            "proValue": "THE forward-looking differentiator: ANTARES already serves the production Rubin diaSource/diaObject schema with a reliability real/bogus score and full pixel/shape flags. A pro app built on this is Rubin-ready today, not just ZTF.",
            "accuracy": "Rubin photometry is FLUX (nJy-scale psfFlux), not magnitude — convert to AB mag for display (use ant_mag for the normalized value). Times are MjdTai (TAI), distinct from ZTF's UTC MJD; account for the offset. Rubin is ramping (800k alerts on 2026-02-24, scaling toward ~7-10M/night) so coverage/depth is still growing.",
            "effort": "Medium–High — flux→mag conversion, TAI handling, and a much larger flag vocabulary than ZTF.",
            "source": "https://api.antares.noirlab.edu/v1/loci/{lsst_locus}/alerts ; https://rubinobservatory.org/for-scientists/data-products/alerts-and-brokers ; https://rtn-090.lsst.io/"
          },
          {
            "title": "Cutout stamps — science / template / difference PNGs (and ZTF candidate/LSST thumbnails)",
            "detail": "VERIFIED LIVE: each locus listing carries meta.newest_thumbnails = 3 alert_thumbnail objects with thumbnail_type in {science, template, difference}, filemime image/png, and a direct public 'src' URL on Google Cloud Storage, e.g. https://storage.googleapis.com/antares-production-ztf-stamps/candid<...>_targ_sci.fits.png (and _targ_diff / _ref). These are pre-rendered PNGs of the FITS cutouts, publicly fetchable with no auth. Client helper get_thumbnails('ztf_candidate:<candid>') or get_thumbnails('lsst:<diaObjectId>') retrieves per-alert stamps.",
            "dataOrApi": "locus meta.newest_thumbnails[].src → direct GCS PNG (no auth, CORS-friendly img src). get_thumbnails(id) in client for per-alert stamps.",
            "proValue": "The science/template/difference triptych is the single most important human-vetting visual for a transient — lets the app show 'is this real?' at a glance next to drb/reliability.",
            "accuracy": "src points at pre-rendered PNGs (not raw FITS); fine for display, not for photometry. 'newest_thumbnails' are for the most recent alert only — fetch per-alert thumbnails for history.",
            "effort": "Low — they are plain <img src> PNG URLs.",
            "source": "https://api.antares.noirlab.edu/v1/loci?page[limit]=1 (meta.newest_thumbnails) ; https://nsf-noirlab.gitlab.io/csdc/antares/client/tutorial/searching.html"
          },
          {
            "title": "Watchlists, saved searches, notifications & subscriptions — portal/Kafka only, NO public REST",
            "detail": "The Locus model carries watch_list_ids and watch_object_ids (so watchlist membership is exposed ON a locus), and filters can match against WatchLists/WatchObjects. BUT watchlist creation/management, saved searches, and notifications are NOT available as public REST endpoints — probed /v1/watchlists, /v1/watch_lists, /v1/subscriptions, /v1/notifications, /v1/streams all return HTTP 404 (only /v1/filters exists, 200). Watchlists/favorites/saved filters are created in the authenticated ANTARES web portal. Real-time delivery is via: (a) ANTARES Kafka OUTPUT STREAMS keyed by tag (requires ANTARES-issued credentials, consumed server-side, not browser), and (b) Slack notifications by request to the ANTARES team. There is no self-serve push/webhook/email subscription API.",
            "dataOrApi": "Read-only: locus.attributes has watch_list_ids/watch_object_ids and grav_wave_events. Write/subscribe: web portal (auth) + Kafka (credentialed, server-side) + Slack (manual request). No public REST for create/notify.",
            "proValue": "Sets correct expectations: a pro app can DISPLAY watchlist/GW associations from locus data and can poll the API for 'new since last check', but true real-time push requires running a credentialed Kafka consumer in the backend, not browser calls.",
            "accuracy": "Do not advertise REST watchlist/subscription creation — it does not exist publicly. For 'alerts to me', implement either backend Kafka stream consumption (ANTARES creds) or periodic polling of /v1/loci sorted by newest_alert_observation_time. grav_wave_events ties loci to LIGO/Virgo GraceDB events (multi-messenger follow-up).",
            "effort": "Polling: Low (backend cron over sort=-newest). Real-time Kafka: High (credentials + persistent consumer). In-browser push: not possible.",
            "source": "Live probes of /v1/* (404s) ; https://nsf-noirlab.gitlab.io/csdc/antares/client/ ; https://iopscience.iop.org/article/10.3847/1538-3881/abd703"
          },
          {
            "title": "Survey coverage & alert volume — ZTF (production, years) + Rubin/LSST (live, ramping to ~10M/night)",
            "detail": "ANTARES is one of the ~7 full-stream LSST/Rubin community brokers and has run ZTF in production since 2019 (announced ANTARES filtering+streaming ZTF). ZTF delivers on the order of ~10^5-10^6 alerts/night (3 bands g/r/i). Rubin/LSST: first real-time alerts launched Feb 2026 (~800,000 alerts on the night of 2026-02-24), ramping toward an eventual ~7-10 million alerts/night across u,g,r,i,z,y; ANTARES scale-tested at the 10M/night Rubin level. Both surveys' loci/alerts are queryable through the SAME endpoints (survey field = 'ztf' or 'lsst'); confirmed live that Rubin loci (ANT2026...) are already returned.",
            "dataOrApi": "Same /v1/loci + /alerts endpoints; discriminate by alert 'survey' field and the ztf_*/lsst_* property namespaces. Tags uniform_random_sample (1% of LSST) and lsst_scimma_quality_transient gauge/curate Rubin volume.",
            "proValue": "Lets the app message a real, current edge: unified ZTF+Rubin access today, positioned for the full LSST firehose. Volume numbers justify why broker-side tag filtering (not raw streams) is the right UX.",
            "accuracy": "Rubin numbers are ramp-phase as of mid-2026 — 800k/night now, design ~10M/night; quote as 'ramping' not steady-state. ZTF and Rubin use different photometric systems (ZTF mag vs Rubin nJy flux) and time standards (UTC MJD vs TAI MJD) — normalize before co-plotting.",
            "effort": "N/A (informational) — drives product positioning and stream-design decisions.",
            "source": "https://noirlab.edu/science/news/announcements/sci19003-1 ; https://rubinobservatory.org/news/first-alerts ; https://www.lsst.org/node/1151 ; https://rtn-090.lsst.io/"
          }
        ]
      },
      {
        "summary": "Both features the user asked for map cleanly onto the app's existing PHASE-7 transient architecture and onto public, browser-direct broker APIs — no backend or token strictly required for a v1, though full-fidelity ZTF forced photometry does require a tokened service. RECOMMENDED LAYOUT for the transient detail panel: (1) a horizontal CUTOUT-TRIPLET VIEWER showing science | template | difference side-by-side (the diagnostic unit pros actually inspect — a clean diff with a point source = real; dipoles/donuts/edge artifacts = bogus), with a REAL/BOGUS BADGE (drb 0–1, braai DL classifier) shown as a colored confidence pill, and a NEGATIVE-SUBTRACTION indicator (isdiffpos) since negative subtractions (source faded relative to template) are scientifically meaningful, not errors; (2) below it, a FORCED-PHOTOMETRY LIGHT CURVE plotting flux vs time per band with error bars AND downward upper-limit arrows at non-detection epochs (5-sigma), with a flux/magnitude toggle — flux (nJy or DN) must be the native space because negative difference fluxes are real and vanish under magnitude conversion. KEY ACCURACY RULES, all source-verified: ZTF mag = zpdiff - 2.5*log10(forcediffimflux) for detections (SNR=forcediffimflux/forcediffimfluxunc > SNT=3), and 5-sigma upper limit = zpdiff - 2.5*log10(SNU*forcediffimfluxunc) with SNU=5 for non-detections; LSST works natively in nJy (mag_AB = 31.4 - 2.5*log10(flux_nJy)). What is browser-direct TODAY: ZTF science/template/difference PNG cutouts from ALeRCE (avro.alerce.online/get_stamp), ZTF + LSST light curves incl. forced photometry from ALeRCE/Fink REST (CORS to verify), drb/real-bogus scores ride in the alert and are exposed by all three brokers. What needs a token/backend: the ZTF Forced Photometry Service (ztfweb.ipac.caltech.edu) which computes on-demand forced LCs at arbitrary positions — email-registered, password-auth, batch up to 1500 positions, async turnaround — and any Rubin RSP pixel/catalog data (data-rights gated). Recommendation: ship cutout-triplet viewer + drb badge + isdiffpos indicator + broker-served forced-photometry LC (browser-direct) in v1; treat the IPAC ZFPS as a 'deep-link out / future backend' seam, never inline.",
        "items": [
          {
            "title": "Alert cutout-triplet viewer (science | template | difference)",
            "detail": "Core difference-imaging UI. Difference imaging produces a triplet per alert: the SCIENCE image (new exposure), a co-added TEMPLATE/reference of the static sky, and the DIFFERENCE = science - template (a PSF-matched subtraction, e.g. via ZOGY proper image subtraction, Zackay/Ofek/Gal-Yam 2016). A real transient appears as a clean PSF-shaped point source in the difference with a flat background; artifacts (cosmic rays, bad subtraction, dipoles from misregistration, ghosts, satellite trails) look wrong in the diff. Pros vet candidates by eyeballing this triplet. In-app: render the three ~63x63 px stamps side-by-side with a shared zoom/stretch (asinh/zscale) and a crosshair at the candidate position. ZTF alert packets carry cutoutScience/cutoutTemplate/cutoutDifference as gzip-compressed FITS (stampData bytes + fileName). LSST/Rubin alert packets carry the analogous cutoutScience/cutoutTemplate/cutoutDifference postage stamps.",
            "dataOrApi": "BROWSER-DIRECT for ZTF: ALeRCE stamps GET avro.alerce.online/get_stamp?oid=<ZTFid>&candid=<candid>&format=png&type=science|template|difference — returns a PNG usable directly as an <img> src (no FITS decode needed). Verify CORS/mixed-content (endpoint is http; proxy or use https host if blocked). For Rubin/LSST alerts, cutouts arrive as gzip-FITS inside the Avro packet and are surfaced via brokers (ALeRCE/Fink/Lasair); a browser FITS-stamp decoder (or broker-provided PNG) is needed. App already plans a stamps step (PHASE-7 step 5.3) with a hips2fits 'archival sky' fallback when real stamps are unavailable.",
            "accuracy": "Label the three panels exactly science/template/difference; never relabel the diff as 'the image'. Apply identical sky orientation (N up/E left) and a per-triplet stretch (asinh or zscale), NOT independent autostretch per panel (would hide the subtraction residual). Center crosshair on the candidate RA/Dec. If only an archival HiPS cutout is shown as fallback, label it 'archival sky at this position', not 'difference image'.",
            "effort": "Low-medium for ZTF (three <img> tags + PNG endpoint + zoom). Medium-high if decoding gzip-FITS stamps client-side (pako + a minimal FITS reader + asinh stretch to canvas) for Rubin alerts.",
            "proValue": "The science/template/difference triplet is THE primary human vetting artifact in time-domain astronomy; scanners and follow-up decisions hinge on whether the difference shows a believable point source. Surfacing it makes the app a real candidate-vetting tool, not just a marker map.",
            "source": "https://zwickytransientfacility.github.io/ztf-avro-alert/schema.html ; https://github.com/alercebroker/usecases/blob/master/api/ALeRCE%20AVRO-STAMP%20Access%20API.md ; https://alerce.readthedocs.io/en/stable/tutorials/stamps_api.html ; ZOGY: https://arxiv.org/pdf/1601.02655 and https://github.com/pmvreeswijk/ZOGY"
          },
          {
            "title": "Real/Bogus (drb) confidence badge on each alert",
            "detail": "Every ZTF alert carries machine real/bogus scores rating whether the difference detection is a real astrophysical source vs an artifact. Fields: drb [0..1] = deep-learning RealBogus score from the 'braai' CNN (closer to 1 = more reliable), with drbversion identifying the model; an older random-forest score rb [0..1] with rbversion also exists. Rubin/LSST will carry an analogous reliability score. In-app: show a colored pill, e.g. 'real-bogus 0.98' green / mid-range amber / low red, with a tooltip naming the classifier+version. A common pro convention is drb > 0.5 (Fink uses RB > 0.55) as a 'likely real' cut — expose the number, optionally a 'likely real / low confidence' label, but always the raw score.",
            "dataOrApi": "BROWSER-DIRECT: drb/rb travel inside the alert candidate record and are exposed by ALeRCE, Fink, Lasair and ANTARES object/alert REST endpoints (e.g. ALeRCE api.alerce.online object + detections; Fink api.fink-portal.org objects). For the LSST stream the app's existing ALeRCE/Fink LSST adapters (PHASE-7) carry the equivalent reliability field.",
            "accuracy": "Display the numeric score and classifier version; do NOT render a binary 'REAL' verdict from a threshold without showing the score and the cut used. drb is per-detection (per candid), not per-object — show it for the alert/detection being viewed. Distinguish drb (deep learning, braai) from legacy rb (random forest) if both present.",
            "effort": "Low — one numeric field already present in the alert/object payload the app fetches; render as a styled pill.",
            "proValue": "Real/bogus scoring is how surveys suppress the ~majority of bogus alerts (cosmic rays, satellites, subtraction artifacts); pros filter and prioritize on it constantly. A visible score signals data honesty and lets users triage.",
            "source": "https://zwickytransientfacility.github.io/ztf-avro-alert/schema.html ; braai real-bogus: https://academic.oup.com/mnras/article/489/3/3582/5554758 and https://arxiv.org/pdf/1907.11259 ; Fink RB>0.55 cut: https://lasair.readthedocs.io/en/main/more_info/faqs.html"
          },
          {
            "title": "Positive/negative subtraction indicator (isdiffpos)",
            "detail": "A difference detection has a SIGN. ZTF field isdiffpos: 't'/'1' => positive subtraction (science minus reference; source brightened / new flux appeared) ; 'f'/'0' => negative subtraction (reference minus science; source FADED relative to the template). Negative subtractions are scientifically real (a star dimming, a variable at minimum, a previously-bright nucleus fading) — they must not be treated as errors or hidden. In-app: a small + / - glyph or up/down arrow on the marker/detail, and in the light-curve plot, render negative-flux epochs distinctly (e.g. below the zero line) rather than dropping them.",
            "dataOrApi": "BROWSER-DIRECT: isdiffpos is in the ZTF alert candidate record (exposed by ALeRCE/Fink/Lasair/ANTARES). For LSST, the sign is implicit in psfDiffFlux being negative (no separate flag needed).",
            "accuracy": "Map isdiffpos correctly: 't'/'1' = positive (brightening), 'f'/'0' = negative (fading). The app's planned filter that 'drops non-positive flux points' (PHASE-7 step 2.3) is a magnitude-display convenience only — keep negative/neg-subtraction points in the underlying flux data and render them; do not silently discard a fading source.",
            "effort": "Low — boolean/string field already in payload; small UI glyph + don't-drop-negatives rule in the plot.",
            "proValue": "Whether a source is appearing vs disappearing is first-order science (SN rise vs decline, dwarf-nova outburst vs return to quiescence, AGN dimming). Pros need the sign; many casual tools wrongly hide negatives.",
            "source": "https://zwickytransientfacility.github.io/ztf-avro-alert/schema.html (isdiffpos field) ; LSST negative psfDiffFlux: https://dp1.lsst.io/products/catalogs/dia_forced_source.html ; https://community.lsst.org/t/difference-versus-forced-flux/11753"
          },
          {
            "title": "Forced-photometry light curve (flux + error bars + 5-sigma upper-limit arrows)",
            "detail": "Forced PSF photometry = measuring PSF-fit flux at a FIXED sky position in every (difference) image, whether or not a source was detected there. This yields a complete light curve including non-detections and pre-discovery history at consistent astrometry — what pros need for rise times, peaks, color evolution, and limits. Plot: x = time (MJD), y = flux per band; DETECTIONS (SNR>threshold) as points with vertical error bars; NON-DETECTIONS as downward-pointing 5-sigma upper-limit arrows at the limiting flux/mag; negative difference fluxes plotted (not dropped). Provide a FLUX <-> magnitude toggle, defaulting to flux because negative/low-SNR points are only representable in flux space. Use the ugrizy (LSST) / gri (ZTF) band color convention the app already defines.",
            "dataOrApi": "BROWSER-DIRECT (broker-served): ALeRCE ZTF REST api.alerce.online object lightcurve returns detections + non_detections + forced_photometry arrays; ALeRCE LSST lightcurve_api (api-lsst.alerce.online/lightcurve_api/detections|forced-photometry, already in PHASE-7 step 2.3) and Fink sources endpoints carry forced/DIA photometry. LSST DP1 DiaForcedSource / ForcedSourceOnDiaObject (psfFlux, psfFluxErr, psfDiffFlux, psfDiffFluxErr, scienceFlux, band, midpointMjdTai, diaObjectId) is RSP/TAP-gated (data rights). TOKEN/BACKEND ONLY for arbitrary-position ZTF: the IPAC ZTF Forced Photometry Service computes forced LCs on demand at any RA/Dec.",
            "accuracy": "ZTF detection mag = zpdiff - 2.5*log10(forcediffimflux); detection when SNR = forcediffimflux/forcediffimfluxunc > SNT (recommended SNT=3). Non-detection 5-sigma upper limit mag = zpdiff - 2.5*log10(SNU*forcediffimfluxunc) with SNU=5. ZTF forcediffimflux/forcediffimfluxunc are in DN; per-epoch zeropoint zpdiff varies, apply per point. LSST fluxes are nJy: mag_AB = 31.4 - 2.5*log10(flux_nJy) (i.e. -2.5*log10(flux_nJy*1e-9/3631)). ZTF forced LCs may need baseline/zero-point offset correction (median of >=30 quiescent epochs subtracted) before use — note this rather than silently assuming zero baseline. Magnitude axis MUST be inverted (brighter up). Always carry magerr/fluxerr; never plot a detection without its error bar.",
            "effort": "Medium — extends the app's planned SVG sparkline (PHASE-7 step 5.2) into a proper plot: add error bars, upper-limit arrow glyphs, a flux/mag toggle, and the per-epoch zeropoint math. Browser-direct via brokers; no new infra.",
            "proValue": "Forced photometry is the standard professional product for transient/variable light curves: it gives pre-discovery limits, smooth coverage through non-detections, and homogeneous flux at one position — essential for SN typing, peak/rise measurement, and stacking. Error bars + upper-limit arrows are the expected presentation in any pro LC plot.",
            "source": "https://arxiv.org/abs/2305.16279 and https://arxiv.org/html/2305.16279 (ZTF FPS: zpdiff, SNT=3, SNU=5, baseline) ; https://web.ipac.caltech.edu/staff/fmasci/ztf/forcedphot.pdf ; https://dp1.lsst.io/products/catalogs/dia_forced_source.html ; https://dp1.lsst.io/tutorials/notebook/201/notebook-201-6.html"
          },
          {
            "title": "ZTF Forced Photometry Service deep-link / future-backend seam (token-gated)",
            "detail": "For forced photometry at an ARBITRARY user-chosen position (not just at existing broker objects), the authoritative source is IPAC's ZTF Forced Photometry Service (ZFPS). It is NOT browser-direct: a user registers an email, receives a password, and submits batch requests (up to 1500 positions per submission, up to 15000 distinct positions in the system; same positions not resubmittable within 90 days) via an HTTP POST script to ztfweb.ipac.caltech.edu/batchfp.html; results return asynchronously as a lightcurve table (forcediffimflux, forcediffimfluxunc, zpdiff, jd, diffmaglim, procstatus...). Recommendation: do NOT inline this in a $0/no-backend app. Provide a 'Get full forced-photometry light curve (ZTF FPS)' deep link / instructions, and design a CutoutService-style auth seam (optional async token provider) so a future thin backend/worker can fulfill it without a refactor — mirroring the app's existing optional authHeaders hook (PHASE-7 step 7).",
            "dataOrApi": "TOKEN + BACKEND: ZTF Forced Photometry Service, ztfweb.ipac.caltech.edu/batchfp.html (email-registered, password auth; public universal account ztffps / 'dontgocrazy!'). Async, polled results — incompatible with pure client-side CORS and not suitable to call from the browser with shared credentials. LSST equivalent (forced photometry on arbitrary positions) will require Rubin Science Platform data rights, also out of scope for anonymous browser use.",
            "accuracy": "Same flux/zeropoint/upper-limit conventions as the broker LC item (zpdiff, SNT=3, SNU=5, DN flux, baseline correction). Do not embed shared credentials in client code shipped to users; route through a backend if/when added. Respect the 90-day resubmission and rate constraints.",
            "effort": "Low to stub (deep link + auth-seam interface, no implementation) ; Medium-high to actually fulfill (requires a small backend/worker holding credentials + async job polling — explicitly a future phase, gated by the app's no-backend rule).",
            "proValue": "On-demand forced photometry at any coordinate (e.g. a user's own target, a multimessenger error region, a pre-explosion site) is a heavily used professional capability; offering even a correct deep-link keeps pros in-workflow and documents the upgrade path.",
            "source": "https://arxiv.org/abs/2305.16279 ; https://web.ipac.caltech.edu/staff/fmasci/ztf/forcedphot.pdf ; https://tom-toolkit.readthedocs.io/en/2.18.1/managing_data/forced_photometry.html"
          },
          {
            "title": "Browser-direct vs backend/token access map (decision summary)",
            "detail": "BROWSER-DIRECT TODAY (subject to CORS verification, consistent with PHASE-7's CORS gate): ZTF science/template/difference PNG cutouts (ALeRCE get_stamp); ZTF + LSST light curves including detections, non-detections, and forced/DIA photometry (ALeRCE & Fink REST, ANTARES/Lasair); drb/rb real-bogus scores and isdiffpos (ride inside alert/object records). These cover the cutout-triplet viewer, real/bogus badge, negative-subtraction indicator, and a forced-photometry LC for any object the brokers already track — i.e. all four UI features above can ship with zero backend. NEEDS BACKEND/TOKEN: IPAC ZTF Forced Photometry Service (arbitrary-position on-demand forced LCs, email+password, async batch); any Rubin RSP pixel data or DP1/DR catalogs accessed directly (data-rights gated, 401 anonymously — confirmed in PHASE-7). MIXED-CONTENT/CORS CAVEAT: avro.alerce.online/get_stamp is http and some broker endpoints may lack permissive CORS; the app's existing nightly static-snapshot GitHub Action (PHASE-7 step 4) and the documented Cloudflare-Worker-proxy option are the sanctioned $0 mitigations if a needed endpoint is closed.",
            "dataOrApi": "Decision matrix, not a single API. Browser-direct: ALeRCE (api.alerce.online, api-lsst.alerce.online, avro.alerce.online), Fink (api.fink-portal.org, api.lsst.fink-portal.org), ANTARES/Lasair REST. Backend/token: ztfweb.ipac.caltech.edu (ZFPS), Rubin RSP/TAP (data.lsst.cloud).",
            "accuracy": "Verify CORS per endpoint before committing to a direct-fetch path (reuse PHASE-7 step-1 CORS probe). Treat http stamp endpoint as needing https proxy under the app's https origin. Never ship shared IPAC/RSP credentials to the client.",
            "effort": "N/A (planning guidance). Confirms all four proposed UI features are $0/no-backend; only the on-demand ZFPS and Rubin pixel data require infra.",
            "proValue": "Lets the app credibly offer pro-grade difference-imaging and forced-photometry features now, while honestly scoping what awaits data rights — matching how working astronomers already split broker-served vs RSP-gated access.",
            "source": "https://github.com/alercebroker/usecases/blob/master/api/ALeRCE%20AVRO-STAMP%20Access%20API.md ; https://rubinobservatory.org/for-scientists/data-products/alerts-and-brokers ; https://irsa.ipac.caltech.edu/docs/program_interface/ztf_api.html ; PHASE-7 plan /Users/kunalbhatia/Downloads/vr-astronomy-app/plan/PHASE-7-transients-lsst.md"
          }
        ]
      },
      {
        "summary": "RECOMMENDED LAYOUT — a CSS-Grid app shell with named regions and a left icon rail, replacing the current single top-left HUD where everything is stacked (search, surveys, toggles, exposure, Share, mode, catalogs, Tonight, broker, About all live in/around one `#hud` box at top:10/left:10, plus floating legend/liveStatus/hipsStatus all manually offset with bottom:26/30/52px — the exact pattern that produces overlap on resize). The fix is to stop positioning every control with `position:fixed` + hand-tuned offsets and instead let ONE `display:grid` container own the screen, so regions are mathematically tiled and CANNOT overlap.\n\nAdopt the convergent pattern shared by ESASky, Aladin Lite v3, Gaia Sky (new UI), WWT, Stellarium Web and Firefly:\n(1) a TOP BAR (full width) holding the search box at left/center plus global actions (mode toggle, Share, About) at the right — search gets its own grid cell so it can never collide with buttons;\n(2) a LEFT ICON RAIL (~56px) of category buttons — Imagery/Surveys, Overlays (constellations/labels/grid), Catalogs, Alerts/Tonight, Settings — each opening exactly ONE docked drawer at a time (mutually-exclusive accordion = zero panel-vs-panel overlap);\n(3) a single DRAWER region that slides between the rail and the canvas (never floats over it on desktop); the survey ladder, catalog list, and the Tonight broker+class-legend+filter each live as a GROUP inside their drawer;\n(4) a BOTTOM STATUS BAR (full width) for the RA/Dec readout (left), FPS/tile stats (center), and attribution credits (right) — three flex cells that truncate with ellipsis instead of overlapping;\n(5) the 3D canvas fills the remaining center cell.\n\nOverlap is prevented STRUCTURALLY, not by z-index luck: grid template rows/cols define non-overlapping tracks; the canvas is the only element allowed to fill its cell edge-to-edge; every panel is a grid child (desktop) or a bottom sheet that is the single top layer (mobile); `env(safe-area-inset-*)` is applied to the grid container padding ONCE rather than per-widget; and at most one drawer is open at a time.\n\nRESPONSIVE collapse: ~1400px desktop = persistent rail + inline docked drawer + top bar + bottom status all visible; ~820px tablet = rail stays, drawer overlays as a fixed-width left panel with scrim, status bar condenses to 2 cells (readout + credits, stats on tap); ~390px phone = rail collapses into the top bar as a single hamburger/segmented control, drawers become full-width bottom sheets (one at a time), bottom status collapses to a single tappable RA/Dec chip, joystick/gyro keep their reserved bottom-left/bottom-center safe-area slots that the grid leaves empty. Pro/Public mode hides the rail's pro categories (catalogs, alerts, exposure, stats) rather than re-flowing, so the grid skeleton is identical in both modes.",
        "items": [
          {
            "title": "App-shell CSS Grid with named regions (the core overlap-proof skeleton)",
            "detail": "Replace per-widget position:fixed with ONE grid container owning 100vw/100vh. Template: rows = [topbar 48px] [main 1fr] [status 28px]; main columns = [rail 56px] [drawer auto] [canvas 1fr]. grid-template-areas: 'top top top' / 'rail drawer canvas' / 'status status status'. Each control is assigned to exactly one area; tracks never overlap by construction. Apply safe-area padding to the grid container only: padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left). The canvas is the sole element allowed to fill its cell edge-to-edge.",
            "proValue": "A pro needs the sky view maximized but controls always reachable and never occluding data; deterministic regions mean a label or readout never lands on top of a source.",
            "effort": "Medium — rewrite index.html shell + ~150 lines CSS; main.ts keeps its logic, only appendChild targets change from #hud to region ids.",
            "accuracy": "Grid track sizes are non-overlapping by spec (CSS Grid Level 1); no z-index needed between regions.",
            "source": "https://aladin.cds.unistra.fr/AladinLite/doc/ , https://www.cosmos.esa.int/web/esdc/esasky-interface"
          },
          {
            "title": "TOP BAR: search box in its own cell, global actions right-aligned",
            "detail": "Full-width bar, grid-template-columns: [brand auto] [search minmax(180px,420px)] [spacer 1fr] [actions auto]. Search lives in a dedicated track so it cannot collide with the right-side action buttons (Pro/Public mode toggle, Share, About, Enter-VR) even as the window narrows — the 1fr spacer absorbs all slack. This mirrors ESASky (search top-right, resolves SIMBAD/coords) and Aladin's goto control. Move the current Share, About, and PRO/Explore buttons here out of the #hud box.",
            "proValue": "Search is the #1 action (name/coord/bibcode resolve); pros expect it persistent and never reflowing under other controls.",
            "effort": "Low.",
            "accuracy": "Search should accept object name, equatorial+galactic coords (match ESASky); keep current SIMBAD/Sesame resolve in ObjectPanel.",
            "dataOrApi": "Existing ObjectPanel.identifyAt / Sesame resolve; no new API.",
            "source": "https://www.cosmos.esa.int/web/esdc/esasky-interface"
          },
          {
            "title": "LEFT ICON RAIL: 5 category buttons, mutually-exclusive drawers",
            "detail": "~56px vertical rail of icon buttons, directly from Gaia Sky's new UI (left-anchored buttons, one per pane) and ESASky's left menu (imaging/catalogue/spectra icons with live counts). Groups for THIS app: (1) Imagery/Surveys [satellite icon], (2) Overlays [grid/line icon: constellations, star labels, coord grid], (3) Catalogs [table icon: Gaia DR3, 2MASS, AllWISE, Chandra — each with its color swatch], (4) Alerts/Tonight [pulse icon: broker toggle + class legend/filter + LIVE indicator], (5) Settings [gear: exposure slider, Pro/Public, attribution detail]. Clicking a button opens its drawer and closes any other — only one drawer ever exists, so panel-vs-panel overlap is impossible. Show a count badge on Catalogs/Alerts like ESASky.",
            "proValue": "Grouping by data role (base imagery vs overlays vs catalogs vs alerts vs tools) is the universal pro mental model; one-at-a-time disclosure keeps the sky unobstructed.",
            "effort": "Medium — build rail + drawer host; re-home existing surveyRow, catRow, toggles, legend, exposure into drawers.",
            "accuracy": "Survey ladder ordering by depth/wavelength (DSS2 → Pan-STARRS/DES/DECaPS → unWISE → Rubin → HST/JWST) and per-survey hemisphere/resolution tooltips already exist in config/surveys.ts — preserve them.",
            "source": "https://gaia.ari.uni-heidelberg.de/gaiasky/docs/3.6.9/User-interface.html , https://www.cosmos.esa.int/web/esdc/esasky-interface"
          },
          {
            "title": "DRAWER region: docked on desktop, bottom-sheet on mobile (one open at a time)",
            "detail": "Desktop/tablet: drawer is a grid column between rail and canvas (Gaia Sky panes, ESASky left data panel that 'docks on the left'), ~300px, internally scrollable, so it pushes/reveals rather than floating over the canvas — no overlap with sky data. Tablet ≤820px: drawer becomes position-fixed left panel with a dim scrim (still single-instance). Phone ≤390px: drawer becomes a full-width bottom sheet that is the only top layer. The survey ladder, catalog chips, and the Tonight broker+legend+filter each render as a titled GROUP inside their respective drawer (replacing today's free-floating legend at bottom:52px and liveStatus at bottom:30px which currently rely on manual offsets to avoid the attribution line).",
            "proValue": "Docked (not floating) panels are the Firefly/ESASky pro convention — data stays visible beside controls; collapsing the legend into the Alerts drawer removes the fragile hand-tuned bottom offsets.",
            "effort": "Medium.",
            "accuracy": "Keep alert class colors (GROUP_COLOR) and per-class filter semantics; keep ZTF/ALeRCE ⇄ ANTARES/LSST broker copy from main.ts.",
            "source": "https://www.cosmos.esa.int/web/esdc/esasky-interface , https://github.com/Caltech-IPAC/firefly"
          },
          {
            "title": "BOTTOM STATUS BAR: RA/Dec | FPS/tiles | credits as 3 flex cells",
            "detail": "Full-width 28px bar, display:flex justify-content:space-between. Left cell = RA/Dec readout (currently #readout, today buried in the HUD box); center = FPS + HiPS order/tile count (currently the floating hipsStatus at bottom:26px); right = attribution credits (currently #attrib at bottom:8px, max-width:60vw). Each cell uses min-width:0 + text-overflow:ellipsis so on narrow widths they truncate instead of overlapping. This is the Gaia Sky bottom-right focus/camera info panel + WWT lower panel + Stellarium status convention. On phone the bar collapses to a single tappable RA/Dec chip; stats/credits move into the Settings drawer.",
            "proValue": "Live RA/Dec, FPS and tile LOD are the pro telemetry pros watch constantly; a fixed status strip is expected and must never sit under other UI.",
            "effort": "Low.",
            "accuracy": "Keep RA in h m and Dec in ° ′ (current readout format); FPS from StatsHud; tile text from hips.order/tileCount already in the loop.",
            "source": "https://gaia.ari.uni-heidelberg.de/gaiasky/docs/3.6.9/User-interface.html , https://worldwidetelescope.org/webclient/"
          },
          {
            "title": "Responsive breakpoints with explicit collapse rules",
            "detail": "≥1400px desktop: rail + inline docked drawer + top bar + 3-cell status all persistent (full Pro density). ~820px tablet: keep rail; drawer overlays as fixed 300px panel + scrim; status condenses to readout+credits (stats on tap). ~390px phone: rail folds into top bar as a segmented/hamburger control; drawers become full-width bottom sheets, ONE at a time; status collapses to a single RA/Dec chip; reserve empty grid gutters for the existing bottom-left joystick (left:18px) and bottom-center gyro button so the grid never lays content into those safe-area slots. Use container/media queries at 640/820/1100/1400. Pro/Public toggles category VISIBILITY in the rail (hide catalogs/alerts/exposure/stats in Public) without changing the grid skeleton, so layout is identical across modes — no reflow, no overlap regressions.",
            "proValue": "Field/observatory use spans phone-to-4K; predictable collapse means muscle memory survives across devices.",
            "effort": "Medium.",
            "accuracy": "Current phone breakpoint is 640px in CSS but 390px is the true small-phone target — add 390 and 820 stops; today the HUD just drops to top:58px which still overlaps on very small screens.",
            "source": "https://stellarium.org/files/guide.pdf , https://aladin.cds.unistra.fr/AladinLite/doc/"
          },
          {
            "title": "Overlap-prevention strategy (the provable part)",
            "detail": "1) ONE grid container tiles the viewport into non-overlapping tracks; nothing inside uses position:fixed except transient sheets/scrims that are full-layer by design. 2) The canvas is the only edge-to-edge element and sits in its own cell. 3) At most one drawer exists at any time (mutual exclusion enforced in the rail click handler). 4) Mobile bottom sheets are a single top layer with a scrim; they replace, never stack. 5) safe-area insets applied once to the container padding, not per-widget (removes today's repeated env() math in main.ts that drifts out of sync). 6) Status/credits cells use min-width:0 + ellipsis so text truncates rather than collides. 7) Reserved empty gutters for joystick/gyro so dynamic controls never land on chrome. Net: overlap is structurally impossible because every pixel belongs to exactly one grid cell, and the only overlays are intentional single full-screen layers (About modal, mobile sheet+scrim).",
            "proValue": "Eliminates the entire class of bugs the current bottom:26/30/52px hand-offsets and z-index 10/11/12 stack produce on resize.",
            "effort": "Design rule, ~0 extra cost once the grid exists.",
            "accuracy": "z-index only needed for the 3 intentional overlays (modal > sheet > scrim); region-to-region needs none.",
            "source": "https://developer.mozilla.org/en-US/docs/Web/CSS/CSS_grid_layout"
          },
          {
            "title": "Color, typography & iconography for the dark data-dense UI",
            "detail": "Keep the existing dark palette but formalize it as CSS variables: --bg #060c18 / panel rgba(6,12,24,.62) with backdrop-blur(6px); --accent #9cc4ff (titles/active), --text #cfe3ff, --muted #7f93b5; active control = rgba(90,140,230,.7) + #9cc4ff border (already in index.html). Typography: ui-monospace for all numeric/coordinate telemetry (RA/Dec, FPS, tiles) — matches Gaia Sky/Firefly data density — but switch panel TITLES and labels to a UI sans (system-ui) so the chrome reads as software, not a terminal. Replace emoji icons (◎ Tonight, ⚡, 🔭, 📱, ⌁) with a consistent monochrome SVG icon set (one stroke weight) so the rail looks professional like ESASky/Gaia Sky. Catalog chips keep per-survey color swatches (preset.color) as the legend key.",
            "proValue": "Monospace-for-numbers + sans-for-labels is the readability convention in pro tools; consistent SVG icons signal a serious instrument, not a toy.",
            "effort": "Low-Medium (icon set is the bulk).",
            "accuracy": "Preserve exact alert-class colors (GROUP_COLOR) and catalog colors as the data legend — these are semantic, not decorative.",
            "source": "https://github.com/Caltech-IPAC/firefly , https://gaia.ari.uni-heidelberg.de/gaiasky/docs/3.6.9/User-interface.html"
          },
          {
            "title": "Where each current control lands (explicit mapping)",
            "detail": "text search → TOP BAR search cell. Survey ladder (DSS2/Pan-STARRS/DES/DECaPS/unWISE/Rubin/HST/JWST) → Imagery drawer (rail btn 1), grouped as a radio ladder. Constellation + star-label toggles + coord grid → Overlays drawer (rail btn 2). Catalog overlays (Gaia DR3/2MASS/AllWISE/Chandra) → Catalogs drawer (rail btn 3) with color swatches + count badge. Tonight live alerts + broker toggle + class legend/filter + LIVE indicator → Alerts drawer (rail btn 4). Exposure slider + Pro/Public + Return-to-Earth → Settings drawer (rail btn 5) [Pro/Public ALSO mirrored as a top-bar toggle]. Share + About → TOP BAR right actions. RA/Dec readout → status bar left. FPS/tile stats → status bar center. Attribution credits → status bar right. Joystick + gyro (touch) → reserved bottom gutters. This empties the overloaded #hud box entirely.",
            "proValue": "Every existing feature keeps a home; nothing is dropped, and each lands in its semantically correct group.",
            "effort": "Medium — mechanical re-parenting in main.ts.",
            "accuracy": "Survey ladder is single-select (radio) for base imagery; catalogs/overlays/alert-classes are multi-select toggles — preserve those distinct interaction models.",
            "source": "https://www.cosmos.esa.int/web/esdc/esasky-interface , https://aladin.cds.unistra.fr/AladinLite/doc/"
          }
        ]
      },
      {
        "items": [
          {
            "title": "Observability engine: RA/Dec + GPS + UTC -> Alt/Az/Airmass, with rise/set/transit and a tonight altitude curve",
            "detail": "Core pipeline: (1) compute Local Sidereal Time (see LST item); (2) hour angle H = LST - RA (wrap to [-12h,12h] or [-pi,pi]); (3) alt/az via the standard transform (see transforms item); (4) airmass from altitude. Rise/set/transit: transit when H=0 (object on meridian, local-time = (RA - LST_at_0h)/sidereal_rate, iterate); rise/set hour angle H0 from cos H0 = (sin h0 - sin(phi) sin(dec)) / (cos(phi) cos(dec)), where phi=latitude, h0=standard altitude at the horizon. Use h0 = -0.5667 deg for stars/planets (mean refraction ~34 arcmin), -0.8333 deg for the Sun (adds 16 arcmin semidiameter), +0.125 deg for the Moon. If cos H0 > 1 -> never rises (circumpolar-below); < -1 -> never sets (circumpolar). rise = transit - H0, set = transit + H0 (convert H0 from deg to sidereal time). Iterate once or twice because RA/Dec of Moon/planets change during the day. Tonight altitude-vs-time curve: sample alt(t) every ~5-10 min from local sunset to sunrise (compute Sun altitude to find civil/nautical/astronomical twilight at Sun alt = -6/-12/-18 deg), plot alt and shade twilight bands; mark transit (max alt = 90 - |phi - dec|). PITFALLS: refraction is variable (depends on T, P, wavelength) so rise/set times are good to ~1 min only; the -0.5667 value assumes 1010 mbar / 10 C. Near the horizon Bennett/Saemundsson refraction formulas are needed if you draw refracted positions. Always carry the date in UTC and convert observer local time via IANA tz, not a fixed offset (DST).",
            "source": "https://aa.usno.navy.mil/faq/alt_az ; Meeus, Astronomical Algorithms 2nd ed. ch.15 (rise/set, h0 values) https://celestialprogramming.com/risesetalgorithm.html",
            "accuracy": "h0 = -0.5667 deg (stars/planets), -0.8333 deg (Sun), +0.125 deg (Moon). cos H0 = (sin h0 - sin phi sin dec)/(cos phi cos dec). Max altitude at transit = 90deg - |phi - dec|. Refraction-limited to ~1 min in time.",
            "dataOrApi": "Pure client-side math; no API needed. Validate against astropy.coordinates AltAz / astroplan Observer.target_rise_time as ground truth (sub-arcminute agreement expected).",
            "proValue": "Pros need to know exactly when a target clears their airmass limit (often X<2.0, alt>30 deg) and how long it stays observable in dark time tonight -- this is the single most-used planning view.",
            "effort": "Medium. ~1-2 days once LST + transforms exist; the iterate-for-Moon/planets refinement adds polish."
          },
          {
            "title": "Airmass: plane-parallel sec z vs Kasten-Young (1989) -- offer both, default to KY",
            "detail": "Plane-parallel: X = sec(z) = 1/cos(z) = 1/sin(alt). Simple, exact for a flat homogeneous atmosphere, but diverges to infinity at the horizon and is already ~0.25% too high at z=60deg and badly wrong below alt~10deg. Kasten & Young (1989) empirical fit, accurate to the horizon: X = 1 / [ cos z + 0.50572 * (96.07995 - z)^(-1.6364) ], with z in DEGREES (z = 90 - alt). Equivalent elevation form: X = 1 / [ sin(h) + 0.50572*(h + 6.07995)^(-1.6364) ], h = altitude in degrees. KY gives X~38 at the true horizon (vs infinity for sec z). PITFALLS: (a) z in KY must be the TRUE (geometric) zenith angle, not the refracted apparent one -- mixing them double-counts refraction; (b) the 96.07995 constant means z must be in degrees -- a very common bug is feeding radians; (c) for spectroscopy/extinction note KY is for a standard atmosphere at sea level, real airmass scales weakly with observatory altitude and wavelength; (d) below alt~3deg even KY has several-percent error. Young (1994) has a higher-order refraction-aware variant if you need <0.1% near the horizon.",
            "source": "Kasten, F. & Young, A.T. 1989, Applied Optics 28, 4735 https://opg.optica.org/ao/abstract.cfm?uri=ao-28-22-4735 ; PDF https://h2lca.org/reports/Kasten_1989_Revised_optical_air_mass_tables.pdf ; pvlib reference impl https://pvlib-python.readthedocs.io/en/v0.4.2/generated/pvlib.atmosphere.relativeairmass.html",
            "accuracy": "KY1989: X = 1/[cos z + 0.50572*(96.07995 - z)^(-1.6364)], z in deg. Coeffs a=0.50572, b=6.07995 (-> 96.07995 = 90+b), c=1.6364. sec z error: +0.25% at z=60, unusable below alt~10deg.",
            "dataOrApi": "Pure math. Cross-check: astropy AltAz.secz (plane-parallel) and pvlib relativeairmass(model='kastenyoung1989').",
            "proValue": "Airmass drives extinction, S/N, and differential-color targets. Pros expect KY by the horizon; a sec z curve that blows up looks amateur. Showing both lets users match their reduction pipeline.",
            "effort": "Trivial (a few lines) once altitude is computed."
          },
          {
            "title": "Local Sidereal Time: use IAU-2006 GMST (or at minimum IAU-1982) from UT1, not UTC",
            "detail": "GMST via Earth Rotation Angle (IAU 2000/2006): ERA = 2*pi*(0.7790572732640 + 1.00273781191135448 * Du) rad, Du = JD(UT1) - 2451545.0. Then GMST = ERA + precession polynomial: GMST = ERA + (0.014506 + 4612.156534*t + 1.3915817*t^2 - 0.00000044*t^3 - 0.000029956*t^4 - 0.0000000368*t^5) arcsec, with t = (TT - J2000)/36525 Julian centuries. LST = GMST + observer EAST longitude (longitude positive east; many bugs come from sign/east-west convention). LMST_apparent (LAST) = LST + equation of equinoxes if you need apparent (nutation) -- usually unnecessary for pointing at arcsec level in a viz app. Simpler legacy option (IAU 1982, good to ~0.1 arcsec over decades): GMST(0h UT) then add UT*1.00273790935; or the compact Meeus formula GMST = 280.46061837 + 360.98564736629*(JD_UT1 - 2451545.0) + 0.000387933*T^2 - T^3/38710000, in degrees, mod 360. PITFALLS: (1) GMST is a function of UT1, NOT UTC -- they differ by DUT1 up to +/-0.9 s (-> up to ~0.225 arcmin pointing error). For an app, approximate UT1 ~= UTC (error <1s) and document it; for survey-grade use IERS Bulletin A DUT1. (2) The precession polynomial uses TT, so you need TT = UTC + leap seconds (37s as of 2026) + 32.184s; do NOT feed UTC there. (3) Longitude sign: LST = GMST + lon_east.",
            "source": "IERS Conventions 2010 ch.5 https://iers-conventions.obspm.fr/content/chapter5/icc5.pdf ; ERA/GMST: IAU resolutions https://arxiv.org/pdf/astro-ph/0602086 ; Meeus formula impl https://www.celestialprogramming.com/snippets/greenwichMeanSiderealTime.html",
            "accuracy": "ERA = 2pi(0.7790572732640 + 1.00273781191135448*Du). Meeus GMST(deg) = 280.46061837 + 360.98564736629*d + 0.000387933*T^2 - T^3/3.871e7. LST = GMST + lon_E. UT1~=UTC error <1s; ignoring it costs up to ~0.25 arcmin.",
            "dataOrApi": "Pure math. DUT1 + leap seconds from IERS Bulletin A/C (https://www.iers.org) or bundled finals2000A table; refresh leap seconds rarely. Validate vs astropy Time.sidereal_time('mean').",
            "proValue": "Every alt/az, HA, and meridian calc depends on LST. Sub-arcsecond GMST is what separates a toy from a pro tool; the UT1-vs-UTC and TT distinctions are exactly the things pros check first.",
            "effort": "Low for Meeus formula (hours). Adding real DUT1/leap-second handling is a half-day plus a periodic data refresh."
          },
          {
            "title": "Alt-Az <-> Equatorial (hour-angle) transforms with atan2",
            "detail": "Equatorial->Horizon (inputs: hour angle H = LST - RA, declination dec, latitude phi): sin(alt) = sin(phi) sin(dec) + cos(phi) cos(dec) cos(H); azimuth via atan2 to get full 0-360: az = atan2( -cos(dec) sin(H), sin(dec) cos(phi) - cos(dec) cos(H) sin(phi) ), measured from NORTH increasing toward EAST (N=0, E=90). [Some texts measure az from South; pick one convention and document it -- this is the #1 source of 180-deg errors.] Horizon->Equatorial (inputs alt a, az A from north, phi): sin(dec) = sin(phi) sin(a) + cos(phi) cos(a) cos(A); H = atan2( -sin(A) cos(a), cos(phi) sin(a) - sin(phi) cos(a) cos(A) ); then RA = LST - H. ALWAYS use atan2(num, den) rather than asin/acos for azimuth and hour angle to keep the correct quadrant and avoid domain errors at the poles/zenith. PITFALLS: (a) azimuth origin (N vs S) and direction (E vs W positive) convention; (b) RA and H share units -- convert RA hours to degrees (x15) consistently; (c) at the zenith azimuth is undefined (cos a -> 0) -- guard it; (d) these are topocentric ideal directions; to draw the REFRACTED apparent altitude add R(alt) (e.g. Bennett: R = cot(alt + 7.31/(alt+4.4)) arcmin); (e) for arcsec pointing you also need precession/nutation/aberration and topocentric parallax (Moon especially).",
            "source": "USNO 'Computing Altitude and Azimuth' https://aa.usno.navy.mil/faq/alt_az ; worked impl https://astrogreg.com/convert_ra_dec_to_alt_az.html",
            "accuracy": "sin alt = sin phi sin dec + cos phi cos dec cos H. az = atan2(-cos dec sin H, sin dec cos phi - cos dec cos H sin phi) from North. Inverse symmetric. Use atan2, never bare asin/acos.",
            "dataOrApi": "Pure math. Ground-truth against astropy SkyCoord.transform_to(AltAz).",
            "proValue": "The fundamental pointing transform for any sky chart, telescope go-to overlay, or AR/VR horizon view. Getting the azimuth quadrant and refraction right is table stakes.",
            "effort": "Low (hours), assuming LST exists."
          },
          {
            "title": "Coordinate grids: equatorial (ICRS/J2000), galactic, horizon -- generate as small great-circle polylines",
            "detail": "General method: a grid line of constant coordinate (e.g. fixed RA, varying Dec) is a curve on the unit sphere; sample it at fine steps (e.g. every 1-2 deg), convert each sampled point to the display frame, project, and draw as a polyline. Densely sample so projected curvature looks smooth; break the polyline where it wraps off-screen or crosses the projection's discontinuity (e.g. RA=0/360 seam, or behind-viewer in a 3D dome). EQUATORIAL (ICRS/J2000): ICRS ~ J2000 equatorial to <~0.1 arcsec, fine for grids; label RA in hours (0-24) and Dec in degrees. GALACTIC: transform ICRS<->Galactic with the IAU-1958 frame defined (J2000) by North Galactic Pole RA=192.85948 deg, Dec=+27.12825 deg, and Galactic longitude of the North Celestial Pole l_NCP=122.93192 deg (equivalently ascending-node lon ~32.93192). Use the standard rotation matrix (astropy's is canonical) rather than hand-rolled Euler angles to avoid sign slips. Galactic equator (b=0) and l=0 toward Galactic center are the key reference lines. HORIZON: lines of constant altitude (almucantars) and azimuth; the horizon itself is alt=0 (draw refracted alt=-0.5667 if you want the visible horizon). PITFALLS: (a) don't draw a grid line as a single straight segment between endpoints -- great circles project to curves; subdivide; (b) pole handling: meridians converge -- many converging labels overlap, thin them near poles; (c) state the epoch explicitly (J2000 vs apparent-of-date differ by ~precession, ~50 arcsec/yr accumulates); (d) galactic frame is defined in J2000/FK5-ish terms -- precess your J2000 data, don't mix of-date.",
            "source": "Galactic frame constants & matrix: https://en.wikipedia.org/wiki/Galactic_coordinate_system ; astropy frames (authoritative transforms) https://docs.astropy.org/en/stable/coordinates/index.html",
            "accuracy": "Galactic (J2000): NGP RA=192.85948, Dec=+27.12825 deg; l of NCP = 122.93192 deg. ICRS~J2000 to <0.1 arcsec. Sample grid curves <=1-2 deg/segment; great circles are NOT straight in projection.",
            "dataOrApi": "Pure math + a rotation matrix. Generate grid vertices client-side. Validate frames vs astropy SkyCoord(frame transforms).",
            "proValue": "Pros overlay galactic plane (dust/extinction context), equatorial grid for catalog cross-ID, and horizon grid for what's observable -- all three toggles are expected in a serious sky atlas.",
            "effort": "Medium. Transform math is small; the polyline subdivision, seam-clipping, and label de-cluttering are the real work (~2-3 days for polish)."
          },
          {
            "title": "Field-of-view framing (telescope + eyepiece / sensor) and angular separation",
            "detail": "TRUE field of view: visual: TFOV (deg) ~= AFOV_eyepiece / Magnification, Magnification = focal_length_scope / focal_length_eyepiece. (AFOV/Mag is an approximation; the exact relation uses the eyepiece field stop: TFOV = (field_stop_diameter_mm / scope_focal_length_mm) * (180/pi) -- prefer field-stop when known, it is what manufacturers spec.) IMAGING (sensor): pixel scale (arcsec/px) = 206.265 * pixel_size_um / focal_length_mm; frame width = pixel_scale * Npix_x / 3600 deg (height similarly). Draw the FOV box/circle centered on target, rotated by camera position angle if given. ANGULAR SEPARATION between two RA/Dec points: prefer the Vincenty great-circle form (stable everywhere incl. small separations and antipodes): dsigma = atan2( sqrt( (cos d2 sin(dRA))^2 + (cos d1 sin d2 - sin d1 cos d2 cos(dRA))^2 ), sin d1 sin d2 + cos d1 cos d2 cos(dRA) ), dRA = RA2-RA1. Haversine is also fine and well-conditioned at small angles: dsigma = 2 asin( sqrt( sin^2(dDec/2) + cos d1 cos d2 sin^2(dRA/2) ). AVOID the spherical law of cosines cos(dsigma)=sin d1 sin d2 + cos d1 cos d2 cos(dRA) for SMALL separations -- acos loses precision (catastrophic cancellation) and can NaN past 1.0. Small-angle quick estimate for labels only: sep ~= sqrt( (dRA*cos(dec_mean))^2 + dDec^2 ) (the cos(dec) on the RA difference is essential).",
            "source": "Vincenty/haversine & precision notes https://en.wikipedia.org/wiki/Great-circle_distance ; astropy angular_separation (uses Vincenty) https://docs.astropy.org/en/latest/api/astropy.coordinates.angular_separation.html ; pixel scale 206.265 standard plate-scale constant.",
            "accuracy": "Pixel scale = 206.265*pix_um/FL_mm arcsec/px. TFOV~=AFOV/Mag (use field stop / FL for exact). Vincenty atan2 form for separation (numerically stable). Always multiply dRA by cos(dec). law-of-cosines fails for small sep.",
            "dataOrApi": "Static eyepiece/camera DB (focal length, AFOV/field stop, sensor pixel size & count) -- bundle a JSON; let users add gear. No external API.",
            "proValue": "Framing a target in a specific scope+camera before a night saves hours. Accurate pixel scale and separation (e.g. to check a double star splits, or a target fits the chip) is a daily-use pro feature.",
            "effort": "Low-medium. Math is trivial; building/curating the equipment list and the rotatable overlay UI is the bulk."
          },
          {
            "title": "Phase-folding periodic light curves (ALeRCE/ANTARES period features) -- epoch-folding conventions",
            "detail": "Given a period P and reference epoch t0 (epoch of, e.g., maximum light or primary minimum), fold each observation time t to phase: phi = ((t - t0) / P) mod 1, giving phi in [0,1). Plot mag (or flux) vs phi; optionally duplicate to [0,2) (plot phi and phi+1) so the cycle wraps visibly. For magnitudes invert the y-axis (brighter = up). Bin in phase (e.g. 50-100 bins) with weighted mean + error for a clean folded curve; overlay raw points faintly. ALeRCE computes candidate periods with the multiband MHAOV periodogram and a Periodogram Pseudo-Entropy (PPE) confidence feature; ANTARES exposes similar period features -- ingest their best period (and harmonics: also try P/2, 2P, since eclipsing binaries alias). CONVENTIONS/PITFALLS: (1) TIME SCALE & REFERENCE FRAME: ZTF alert times are MJD; many catalogs use HJD or BJD_TDB. Heliocentric/barycentric correction removes up to +/-8.3 min (light travel across Earth's orbit) -- folding raw UTC/JD of geocentric times smears periods near ~1 yr or short periods. If the survey reports BJD_TDB, keep it; do not silently mix HJD and BJD (differ up to ~4 s) or JD_UTC and JD_TDB (leap-second offset). (2) t0 definition varies (max light vs min light vs ascending node) -- label what phase 0 means. (3) The mod must handle t<t0 (use a positive-remainder mod). (4) Pick the right alias: half-period folding of an eclipsing binary makes the two minima overlap -- show a periodogram or let user toggle P, 2P, P/2. (5) For long campaigns, an imprecise P accumulates phase drift = (N_cycles)*(dP/P); display the period uncertainty.",
            "source": "ALeRCE Light Curve Classifier (MHAOV period, PPE), Sanchez-Saez et al. 2021, AJ https://iopscience.iop.org/article/10.3847/1538-3881/abd5c1 (arXiv https://arxiv.org/abs/2008.03311) ; ANTARES broker https://antares.noirlab.edu ; barycentric correction https://pyastronomy.readthedocs.io/en/latest/pyaslDoc/aslDoc/baryvel.html",
            "accuracy": "phi = ((t - t0)/P) mod 1, positive remainder. Plot [0,2). Heliocentric/barycentric timing matters up to +/-8.3 min (geocentric->barycenter). Keep BJD_TDB; don't mix HJD/BJD or JD_UTC/JD_TDB. Try P, 2P, P/2 for aliases.",
            "dataOrApi": "ALeRCE API https://api.alerce.online (REST + Python client; objects/features/lightcurve endpoints, CORS-friendly JSON). ANTARES https://antares.noirlab.edu (REST + antares-client). Both serve ZTF light curves & period features.",
            "proValue": "Folding a variable/eclipsing-binary/RR-Lyrae light curve at the catalog period is the canonical way to confirm a classification and read amplitude/shape -- a headline pro feature, and ALeRCE/ANTARES periods make it turnkey.",
            "effort": "Medium. Folding + binning is small; plumbing the broker APIs, handling time scales, and an interactive period/epoch slider (with alias toggles) is ~3-4 days."
          },
          {
            "title": "Photometric basics: AB magnitude, zeropoints, mag<->flux, error bars, 5-sigma upper limits",
            "detail": "AB magnitude (Oke & Gunn 1983): m_AB = -2.5 log10(f_nu) - 48.60, with f_nu in erg s^-1 cm^-2 Hz^-1. Zeropoint form: f_nu0 = 3631 Jy = 3.631e-20 erg s^-1 cm^-2 Hz^-1 gives m_AB=0; so f_nu = 3631 Jy * 10^(-0.4 m_AB), and 1 Jy = 1e-23 erg s^-1 cm^-2 Hz^-1. Instrumental mag<->flux with a survey zeropoint ZP (counts): m = ZP - 2.5 log10(counts); invert counts = 10^(-0.4 (m - ZP)). ERROR BARS: convert flux S/N to magnitude error exactly via dm = 2.5 log10(1 + 1/SNR); for SNR>~5 the linear approx dm ~= 1.0857 / SNR holds (1.0857 = 2.5/ln10). Conversely fractional flux error sigma_f/f ~= dm/1.0857. Propagate symmetric flux errors into ASYMMETRIC magnitude error bars (faint side is larger) -- a common pro complaint about plots that show symmetric mag errors. 5-SIGMA UPPER LIMITS (non-detections): m_lim = ZP - 2.5 log10(5 * sigma_f), where sigma_f is the 1-sigma background/flux noise in the aperture; plot non-detections as downward-pointing-arrow upper limits at m_lim, NOT as data points (forced-photometry flux with its error is even better and lets negative flux be shown honestly). PITFALLS: (1) AB vs Vega vs ST zeropoints differ by band (e.g. AB-Vega offset ~ -0.04 in V but ~ +0.9 in K) -- never mix systems on one axis; (2) the -48.60 only works with f_nu in CGS erg units, not SI/Jy directly; (3) ZTF/PS1/SDSS report AB-ish but each has its own ZP per image -- use the per-epoch ZP, not a global one; (4) extinction (Galactic A_lambda and atmospheric airmass*k) must be stated if you de-redden; (5) for upper limits choose and label the sigma (3 vs 5) and aperture.",
            "source": "AB system: Oke & Gunn 1983; https://en.wikipedia.org/wiki/AB_magnitude ; 5-sigma limit & forced photometry: ZTF data system Masci et al. 2019 https://arxiv.org/pdf/1902.01872 ; mag-error/SNR (1.0857) https://www.eso.org/~ohainaut/ccd/sn.html",
            "accuracy": "m_AB=-2.5 log10(f_nu)-48.60 (f_nu cgs); f_nu0=3631 Jy=3.631e-20 cgs. counts<->mag: m=ZP-2.5log10(counts). dm=2.5log10(1+1/SNR)~=1.0857/SNR. 5-sigma limit: m_lim=ZP-2.5log10(5 sigma_f). Mag errors are asymmetric; never mix AB/Vega/ST.",
            "dataOrApi": "ZTF/ALeRCE/ANTARES light curves carry per-epoch magzp/sigmaflux; SDSS/PS1 catalogs are AB. Use survey-supplied ZP per epoch. CORS: ALeRCE/ANTARES REST are browser-callable JSON.",
            "proValue": "Correct mag<->flux, asymmetric error bars, and upper-limit arrows are exactly what distinguishes a pro photometry viewer. Showing 5-sigma limits on non-detections is essential for transient/variable science (rise times, last non-detection).",
            "effort": "Low-medium. Formulas are trivial; doing asymmetric error bars + upper-limit arrows + per-epoch ZP correctly in the plotting layer is ~2 days."
          }
        ],
        "summary": "All six feature areas verified against authoritative sources (USNO, IERS Conventions 2010, Meeus Astronomical Algorithms, Kasten & Young 1989 ApOpt, Oke & Gunn 1983, ALeRCE/ANTARES papers). Key exact formulas confirmed: (1) Airmass -- offer BOTH plane-parallel sec z and Kasten-Young 1989 X = 1/[cos z + 0.50572*(96.07995 - z)^(-1.6364)] with z in DEGREES; default to KY since sec z diverges below ~10 deg altitude. (2) LST -- use IAU-2006 GMST from ERA = 2pi(0.7790572732640 + 1.00273781191135448*Du), or Meeus GMST(deg)=280.46061837+360.98564736629*d+...; LST = GMST + east longitude. (3) Alt/az -- sin alt = sin phi sin dec + cos phi cos dec cos H, azimuth via atan2 (never bare asin/acos). (4) Rise/set -- cos H0 = (sin h0 - sin phi sin dec)/(cos phi cos dec) with h0 = -0.5667 deg stars, -0.8333 Sun, +0.125 Moon. (5) Angular separation -- Vincenty atan2 form (stable everywhere); avoid law-of-cosines for small angles; always multiply dRA by cos(dec). (6) Galactic frame J2000 -- NGP RA=192.85948, Dec=+27.12825, l_NCP=122.93192. (7) Photometry -- m_AB=-2.5log10(f_nu)-48.60 (3631 Jy zeropoint), dm=2.5log10(1+1/SNR)~=1.0857/SNR, 5-sigma limit m_lim=ZP-2.5log10(5 sigma_f). CONCRETE LAYOUT RECOMMENDATION: build a single \"Tonight\" planning panel combining the altitude-vs-time curve (twilight bands at Sun alt -6/-12/-18, transit marker, user airmass-limit line) as the hero; a togglable grid overlay (equatorial J2000 / galactic / horizon, drawn as subdivided great-circle polylines); an FOV framing tool (eyepiece/sensor DB, pixel scale = 206.265*pix_um/FL_mm); and a light-curve panel that pulls ALeRCE/ANTARES periods to phase-fold (phi=((t-t0)/P) mod 1, plot [0,2)) with AB-correct flux<->mag, asymmetric error bars, and 5-sigma upper-limit arrows. CRITICAL CROSS-CUTTING PITFALLS to bake in everywhere: time scales -- GMST polynomial needs TT (=UTC+leap+32.184s, leap=37s in 2026) while ERA needs UT1 (approx UTC, DUT1<0.9s); light-curve folding needs BJD_TDB and barycentric correction (up to +/-8.3 min); never mix AB/Vega/ST magnitude systems; refraction makes rise/set times good only to ~1 min and the standard h0=-0.5667 assumes 1010 mbar/10C; always state the epoch (J2000 vs of-date) and longitude sign convention (east-positive)."
      },
      {
        "items": [
          {
            "title": "SIMBAD (CDS) — TAP + Sesame name resolver: WORKS browser-direct",
            "detail": "SIMBAD's TAP service and the Sesame name resolver both send Access-Control-Allow-Origin: * (verified by live curl with an Origin header). For an object at a known RA/Dec you can run an ADQL cone query against the basic table to get a canonical ID, object type (otype), spectral type, redshift, parallax/distance, B/V mag, and bibliography count. Sesame resolves a name -> RA/Dec instantly (used by astropy's SkyCoord.from_name). Both are fully usable from a static SPA with zero backend.",
            "dataOrApi": "SIMBAD TAP sync: https://simbad.cds.unistra.fr/simbad/sim-tap/sync?request=doQuery&lang=adql&format=json&query=SELECT ... (ADQL, supports CONTAINS(POINT(...),CIRCLE(...)) cone). Sesame: https://cds.unistra.fr/cgi-bin/nph-sesame/-oxp/SNV?{name} (XML) or -oI (plain text). CORS: Access-Control-Allow-Origin:* on both. Browser-direct, no token.",
            "proValue": "Authoritative cross-ID + object classification + curated bibliography for galactic/extragalactic sources; the standard 'what is this object' lookup astronomers trust.",
            "accuracy": "Use format=json or VOTable and parse field metadata; cone radius in degrees; coordinates ICRS J2000. Beware sim-tap row limits (default TOP/maxrec ~ a few thousand).",
            "effort": "Low — single fetch + JSON parse.",
            "source": "https://simbad.cds.unistra.fr/simbad/sim-tap ; https://cds.unistra.fr/cgi-bin/nph-sesame ; verified via curl Access-Control-Allow-Origin:*"
          },
          {
            "title": "VizieR TAP (CDS) — catalog cross-match: WORKS browser-direct",
            "detail": "VizieR's TAP endpoint returns Access-Control-Allow-Origin: * (verified). Lets you ADQL-query thousands of published catalogs (including the Gaia DR3 mirror I/355/gaiadr3, 2MASS, AllWISE, Pan-STARRS, etc.) by cone at RA/Dec. This is the practical CORS-open way to get Gaia DR3 photometry/astrometry into a browser app without a proxy, since the ESA Gaia TAP itself is CORS-blocked (see Gaia entry).",
            "dataOrApi": "https://tapvizier.cds.unistra.fr/TAPVizieR/tap/sync?request=doQuery&lang=adql&format=json&query=SELECT TOP 50 * FROM \"I/355/gaiadr3\" WHERE 1=CONTAINS(POINT('ICRS',RAJ2000,DEJ2000),CIRCLE('ICRS',{ra},{dec},{r_deg})). CORS: Access-Control-Allow-Origin:* . Browser-direct, no token.",
            "proValue": "One ADQL surface over essentially every published catalog; ideal for multi-catalog enrichment (Gaia G/BP/RP, parallax, PM; 2MASS JHK; WISE) at a position.",
            "accuracy": "Table IDs are exact ('I/355/gaiadr3' for Gaia DR3 main); column names are catalog-specific (RAJ2000/DEJ2000). Respect maxrec; request VOTable for typed columns/units. Gaia DR3 epoch 2016.0 — apply proper motion for current-epoch matching.",
            "effort": "Low–medium — ADQL string + JSON/VOTable parse.",
            "source": "https://tapvizier.cds.unistra.fr/TAPVizieR/tap ; verified Access-Control-Allow-Origin:*"
          },
          {
            "title": "Gaia DR3 (ESA gea.esac TAP) — CORS-BLOCKED from browser; use VizieR or Astro Data Lab via proxy",
            "detail": "The canonical ESA Gaia archive TAP (gea.esac.esa.int/tap-server) returns NO Access-Control-Allow-Origin header on GET and 403s the CORS preflight (verified live). A browser fetch completes the network request but JS cannot read the body — a hard CORS failure for a no-backend app. The NOIRLab Astro Data Lab TAP (datalab.noirlab.edu/tap) ALSO returns no ACAO on GET or OPTIONS (verified) — likewise unusable browser-direct. Net: for Gaia DR3 in a pure SPA, go through VizieR TAP (CORS-open, mirror I/355/gaiadr3) OR run any of these behind your own CORS proxy/serverless function.",
            "dataOrApi": "ESA: https://gea.esac.esa.int/tap-server/tap/sync (ADQL gaiadr3.gaia_source) — NO ACAO. Astro Data Lab: https://datalab.noirlab.edu/tap/sync (gaia_dr3.gaia_source) — NO ACAO. Both need a proxy. CORS-open alternative: VizieR I/355/gaiadr3.",
            "proValue": "Gaia DR3 gives 5-parameter astrometry (parallax->distance, proper motion), G/BP/RP photometry, RUWE, radial velocities — the backbone of stellar cross-matching and distance estimation.",
            "accuracy": "Gaia ref epoch J2016.0; propagate PM to obs epoch before matching. ADQL TOP/maxrec limits apply. If proxying, cache aggressively (these archives rate-limit).",
            "effort": "Medium if proxying (write a tiny pass-through that adds ACAO); Low if you just use VizieR instead.",
            "source": "https://gea.esac.esa.int/tap-server/tap (verified no ACAO, OPTIONS 403); https://datalab.noirlab.edu/tap (verified no ACAO)"
          },
          {
            "title": "NED (NASA/IPAC Extragalactic Database) — CORS-BLOCKED, needs proxy",
            "detail": "NED's ObjectLookup and its TAP endpoint return HTTP 200 but send NO Access-Control-Allow-Origin header (verified live with Origin). So a browser app cannot read NED responses directly — requires a backend/serverless proxy. NED is the authoritative source for extragalactic objects: redshifts, redshift-independent distances, cross-IDs, photometry, and references.",
            "dataOrApi": "ObjectLookup: https://ned.ipac.caltech.edu/srs/ObjectLookup?name={name} ; NED TAP: https://ned.ipac.caltech.edu/tap/sync (NEDTAP.objdir). Cone/position: VO cone + 'near position' services. CORS: none -> proxy required.",
            "proValue": "Best-in-class extragalactic enrichment (redshift, distance, galaxy classification, multiwavelength photometry) — complements SIMBAD for galaxies/AGN.",
            "accuracy": "Returns heliocentric redshift; distinguish spectroscopic vs photometric; cross-ID name forms vary (NGC/Messier/2MASX). Position cone radius in arcmin.",
            "effort": "Medium — must stand up a CORS proxy.",
            "source": "https://ned.ipac.caltech.edu/ ; verified ObjectLookup & /tap return 200 with no ACAO"
          },
          {
            "title": "Transient Name Server (TNS) — NOT usable browser-direct (forbidden header + secret key)",
            "detail": "TNS is doubly blocked for a no-backend browser app: (1) every scripted/API request MUST carry a custom User-Agent of the form tns_marker{\"tns_id\":..,\"type\":\"bot\",\"name\":..} or you get HTTP 403 — but User-Agent is a Forbidden Header Name in the Fetch spec, so browsers silently drop/override it and the call fails; (2) the bot API-Key is a secret that must not ship in client JS. A bare OPTIONS to the API already returned 403 (verified). TNS therefore requires a backend/serverless proxy that injects the User-Agent marker and key.",
            "dataOrApi": "POST https://www.wis-tns.org/api/get/object (and /search) with multipart 'api_key' + 'data', plus User-Agent: tns_marker{...}. CORS/auth: forbidden UA header + secret key => proxy mandatory.",
            "proValue": "Canonical registry of supernovae/transients: official IAU name, discovery date/mag, classification, spectra, internal names (ZTF/ATLAS) — essential for 'is this a known transient' at a position+epoch.",
            "accuracy": "Cone search by RA/Dec+radius returns reported transients; respect 60s rate-limit windows; classification may be 'unclassified'.",
            "effort": "Medium–high — needs a server component holding the key.",
            "source": "https://www.wis-tns.org/content/tns-getting-started ; https://corsfix.com/blog/set-custom-user-agent-fetch-javascript (browsers drop custom UA); verified OPTIONS 403"
          },
          {
            "title": "Skybot / SsODNet conesearch (IMCCE) — asteroid ID at position+time: WORKS browser-direct",
            "detail": "The IMCCE Skybot cone-search returns Access-Control-Allow-Origin: * AND Access-Control-Allow-Methods including OPTIONS (verified live) and returned real JSON (e.g. found minor planet '2006 UX18' at the test RA/Dec/epoch). This is the practical browser-direct way to answer 'is there a known solar-system body at this RA/Dec at this time?' — exactly the MPChecker use case, but with proper CORS and JSON.",
            "dataOrApi": "https://ssp.imcce.fr/webservices/skybot/api/conesearch.php?RA={deg}&DEC={deg}&SR={deg}&EPOCH={ISO-or-JD}&-mime=json . CORS: Access-Control-Allow-Origin:* . Browser-direct, no token.",
            "proValue": "Identifies asteroids/comets/planets crossing a field at a given epoch (ephemeris-based), with name, class, V mag, on-sky motion (dRA/dDEC) and positional error — lets a pro rule out a moving object.",
            "accuracy": "EPOCH must be the observation time (UTC/JD); SR is search radius in degrees; positions are computed ephemerides (not detections) so accuracy depends on orbit quality (Err arcsec field). Use for identification, not astrometry.",
            "effort": "Low — single GET + JSON parse.",
            "source": "https://ssp.imcce.fr/webservices/skybot/ ; verified Access-Control-Allow-Origin:* with JSON body"
          },
          {
            "title": "Minor Planet Center (MPChecker vs new data API) — MPChecker is HTML-only; prefer Skybot",
            "detail": "MPC's classic MPChecker (cgi-bin/mpcheck.cgi) does send ACAO:* (verified) but it returns an HTML form/result page, not machine JSON — awkward to parse client-side. The newer MPC data API (data.minorplanetcenter.net/api) is CORS-enabled (echoes Origin, verified) and good for orbits/observations, but it is NOT a 'what's at this RA/Dec/time' identifier. Recommendation: for position+time asteroid identification in a browser, use IMCCE Skybot (above); use the MPC data API only if you need orbital elements/designations.",
            "dataOrApi": "MPChecker (HTML): https://minorplanetcenter.net/cgi-bin/mpcheck.cgi (ACAO:* but HTML). MPC data API: https://data.minorplanetcenter.net/api/... (POST JSON, ACAO echoes Origin). CORS OK on both; only the data API is JSON.",
            "proValue": "MPC is the IAU authority for minor-planet designations/orbits; useful for confirming a designation found via Skybot.",
            "accuracy": "Do not screen-scrape mpcheck.cgi in production; use Skybot for identification and MPC data API for authoritative elements.",
            "effort": "Low (data API) / not recommended (HTML scrape).",
            "source": "https://minorplanetcenter.net/ ; https://minorplanetcenter.net/mpcops/documentation/ ; verified ACAO on both"
          },
          {
            "title": "ZTF / IRSA services — CORS-BLOCKED, needs proxy",
            "detail": "IRSA's VO TAP (irsa.ipac.caltech.edu/TAP) hosting ZTF object/lightcurve tables returns 200 but sends NO Access-Control-Allow-Origin on GET (verified). The OPTIONS preflight returns 200 yet still without ACAO headers, so a real cross-origin browser read fails. ZTF enrichment (object IDs, multi-epoch light curves, alerts) therefore needs a proxy. (The separate ZTF alert/Avro brokers like ANTARES/ALeRCE have their own REST APIs — check those individually if you need alerts.)",
            "dataOrApi": "IRSA TAP: https://irsa.ipac.caltech.edu/TAP/sync (ztf.ztf_objects_* , fp_psc, etc.) — NO ACAO. ZTF lightcurve API: https://irsa.ipac.caltech.edu/cgi-bin/ZTF/nph_light_curves — same IRSA host, treat as proxy-required.",
            "proValue": "ZTF gives dense g/r/i light curves and object cross-IDs for variability/transient context across millions of sources.",
            "accuracy": "If proxying, request FORMAT=votable for typed columns; ZTF mags are in ZTF system; flag catflags for quality.",
            "effort": "Medium — proxy required.",
            "source": "https://irsa.ipac.caltech.edu/TAP ; verified 200 with no ACAO on GET and OPTIONS"
          },
          {
            "title": "hips2fits (CDS) FITS cutout — WORKS browser-direct; the data source for in-browser FITS",
            "detail": "VERIFIED LIVE: https://alasky.cds.unistra.fr/hips-image-services/hips2fits returns Access-Control-Allow-Origin:*, Access-Control-Allow-Methods: GET,OPTIONS, Content-Type: application/fits, and a valid FITS (starts with 'SIMPLE  =  T'). My test pull (DSS2 red, 50x50, fov=0.1deg, TAN, ra=10.68 dec=41.27) came back BITPIX=16, NAXIS1=NAXIS2=50, with a clean CDELT/CTYPE WCS: CRPIX1/2=25, CDELT1=-0.00200, CDELT2=+0.00200, CTYPE1='RA---TAN', CTYPE2='DEC--TAN', CRVAL1=10.68, CRVAL2=41.27. So you can request any HiPS survey (DSS2, PanSTARRS, 2MASS, SDSS, WISE, etc.) as a FITS cutout and parse it client-side.",
            "dataOrApi": "GET https://alasky.cds.unistra.fr/hips-image-services/hips2fits?hips={CDS/P/DSS2/red}&width=&height=&fov={deg}&projection=TAN&coordsys=icrs&ra=&dec=&format=fits . CORS: Access-Control-Allow-Origin:* . Browser-direct.",
            "proValue": "Gives a real, WCS-calibrated science FITS array (not a JPEG) for any sky position from many surveys — enables true pixel readout and scientific stretches in-browser.",
            "accuracy": "Returned WCS here is CDELT+CTYPE (no CD matrix, CROTA absent => no rotation) for TAN projection; honor it exactly for pixel<->sky. format=fits gives raw values; format=jpg/png would destroy photometry. Note BITPIX varies by survey (DSS2=16-bit int; many are -32 float).",
            "effort": "Low to fetch; parsing effort is the FITS work below.",
            "source": "https://alasky.cds.unistra.fr/hips-image-services/hips2fits ; verified ACAO:* + application/fits + SIMPLE header + WCS keywords from a live cutout"
          },
          {
            "title": "FITS parsing in JS: manual DataView parse (recommended) — header blocks, BITPIX, BZERO/BSCALE, big-endian",
            "detail": "FITS is fixed-format and small for cutouts, so a manual ArrayBuffer/DataView parse is the most accurate and dependency-light path. Algorithm: (1) Fetch as arrayBuffer (response.arrayBuffer()). (2) Parse the header as 80-char ASCII 'card images' in 2880-byte blocks; keep reading blocks until a card whose keyword == 'END'; the data unit starts at the next 2880 boundary. (3) Read BITPIX, NAXIS, NAXIS1, NAXIS2, and BZERO (default 0), BSCALE (default 1). (4) Data is row-major, FITS is BIG-ENDIAN, so read with DataView using littleEndian=false: BITPIX 8 => getUint8; 16 => getInt16; 32 => getInt32; -32 => getFloat32; -64 => getFloat64. (5) TRUE physical value = BZERO + BSCALE*raw (this is how DSS2's 16-bit ints become real intensities; for unsigned-16 data FITS uses BZERO=32768). (6) BUNIT card gives units to display. (7) NaN handling: for -32/-64, IEEE NaN means blank/no-data — skip in min/max and render transparent; for integer BITPIX, the BLANK card (if present) marks blanks. Report the unscaled physical value (post BZERO/BSCALE) at the cursor, with BUNIT.",
            "dataOrApi": "No API — Web platform: fetch().arrayBuffer(), DataView(buffer). Card parse: substring(0,8).trim() keyword, value after '='. Pixel(i,j) byte offset = dataStart + (j*NAXIS1 + i)*bytesPerPix.",
            "proValue": "Guarantees you report TRUE pixel values & units (not a renormalized display byte), correct blank/NaN handling, and exact byte order — what a professional needs for any quantitative readout.",
            "accuracy": "Critical correctness points: 2880-byte block alignment; END terminates header; big-endian via DataView(false); apply BZERO/BSCALE; honor BLANK/NaN; FITS pixel coords are 1-based and the array's first pixel is bottom-left (flip Y for canvas top-left).",
            "effort": "Medium — ~150 lines; no deps.",
            "source": "FITS Standard 4.0 (https://fits.gsfc.nasa.gov/fits_standard.html); verified card/block layout against the live hips2fits cutout (2880 header, BITPIX=16)"
          },
          {
            "title": "FITS libraries (fitsjs / astrojs) vs manual — when to use a lib",
            "detail": "astrojs/fitsjs (github.com/astrojs/fitsjs) reads images, cubes, compressed images, and tables, requires the DataView API (Chrome9+/FF15+/Safari6+), reads BITPIX with BZERO(default0)/BSCALE(default1) and applies value=bzero+bscale*raw with endian swap, and supports Web Workers for big files. It's convenient but old (CoffeeScript, ~2013-era, sparsely maintained) and pulls full-file logic you may not need for a 50x50 cutout. fits.js (and JS9, a full DS9-in-browser via Emscripten) are heavier. Recommendation: for hips2fits cutouts, hand-roll the DataView parse (entry above) for full control over true-value readout and NaN/clip behavior; reach for a library only if you need tile-compressed images (RICE/GZIP, BITPIX in ZIMAGE), binary tables, or data cubes — those are tedious to implement. If you want a ready full viewer, use JS9 rather than fitsjs.",
            "dataOrApi": "astrojs/fitsjs: https://github.com/astrojs/fitsjs (MIT). JS9: https://js9.si.edu (full FITS viewer + scales incl. zscale, log, asinh, runs client-side).",
            "proValue": "Libraries save you on compressed/table/cube edge cases and give battle-tested scaling; manual parse gives auditable, exact physical-value readout.",
            "accuracy": "If using fitsjs verify it reports physical (post-BZERO/BSCALE) values for your readout, and that it surfaces NaN/BLANK rather than clamping; test against a known 16-bit-with-BZERO file (e.g. DSS2).",
            "effort": "Low to adopt JS9; Low–Medium for fitsjs; Medium for manual.",
            "source": "https://github.com/astrojs/fitsjs ; http://astrojs.github.io/fitsjs/ ; https://js9.si.edu"
          },
          {
            "title": "WCS pixel<->RA/Dec for accurate cursor readout (CDELT/CROTA and CD-matrix forms)",
            "detail": "Read CRPIX1/CRPIX2 (reference pixel, 1-based), CRVAL1/CRVAL2 (RA/Dec of that pixel, deg), and CTYPE1/2 (e.g. RA---TAN/DEC--TAN). Two linear-transform conventions: (A) CD matrix (CD1_1,CD1_2,CD2_1,CD2_2) — intermediate coords (xi,eta)[deg] = CD * (pix - CRPIX); (B) CDELT1/CDELT2 (+ optional CROTA2 rotation, or PC matrix) — equivalent to a CD built from CDELT and CROTA. The live hips2fits cutout used form (B): CDELT only, no CROTA => no rotation, so xi=CDELT1*(i+1-CRPIX1), eta=CDELT2*(j+1-CRPIX2). Then apply the projection deprojection (TAN/gnomonic) to turn (xi,eta) into (RA,Dec): standard gnomonic inverse about (CRVAL1,CRVAL2). For a cursor readout at the FOVs of a cutout, TAN is what hips2fits returns; implement the exact TAN inverse (not a flat approximation) to stay sub-arcsec. Convert canvas(x,y)->FITS pixel (remember Y flip and 1-based), then pixel->sky.",
            "dataOrApi": "Header cards: CRPIX1/2, CRVAL1/2, CTYPE1/2, plus either CD1_1.. or CDELT1/2(+CROTA2/PC). Projection per CTYPE suffix (TAN here).",
            "proValue": "Gives an accurate live RA/Dec under the cursor — the difference between a toy and a usable finder; pros expect WCS-correct coordinates.",
            "accuracy": "Use 1-based FITS pixels; CRPIX is 1-based; apply true gnomonic (TAN) inverse: it differs from linear CDELT scaling away from CRPIX (matters across a wide FOV). Handle both CD and CDELT/CROTA inputs since surveys differ. If you need full SIP/TPV distortion, use wcs.js/wcslib-emscripten; for plain TAN cutouts hand-rolled is fine.",
            "effort": "Medium — TAN inverse is ~20 lines; handle CD-vs-CDELT branch.",
            "source": "FITS WCS Paper II (Calabretta & Greisen 2002, gnomonic TAN); FITS Standard 4.0 WCS; verified CTYPE='RA---TAN'/CDELT form in live cutout"
          },
          {
            "title": "Scientific stretches: linear, log, asinh (Lupton), and DS9 zscale sampling",
            "detail": "After getting true pixel values, compute display limits then map to 0-255. (1) LINEAR: out=clip((v-z1)/(z2-z1)). (2) LOG: out=log(1+a*(v-z1)/(z2-z1))/log(1+a) (a~1000 typical) — good dynamic range. (3) ASINH (Lupton et al. 2004): out=asinh((v-z1)/beta)/asinh((z2-z1)/beta) where beta sets the linear-to-log transition (a 'softening'); this is the SDSS-standard stretch that preserves faint detail without saturating bright cores. (4) ZSCALE limit-finder (DS9/IRAF, matches astropy ZScaleInterval): sample up to n_samples (default 1000) pixels, sort, fit a line value(rank)=intercept+slope*(rank-midpoint) with ITERATIVE k-sigma rejection (krej=2.5, max_iterations=5, max_reject=0.5). If >50% rejected -> z1,z2 = sample min,max. Else z1=median+(slope/contrast)*(1-midpoint), z2=median+(slope/contrast)*(npix-midpoint), clipped to sample range; contrast default 0.25. zscale gives the pleasant 'background+sources' view DS9 shows by default. Always exclude NaN/BLANK from sampling and from min/max, and clip outputs to [0,1] before scaling to 8-bit.",
            "dataOrApi": "Pure JS over the typed pixel array. Reference impl: astropy.visualization ZScaleInterval (n_samples=1000, contrast=0.25, krej=2.5, max_iterations=5, max_reject=0.5, min_npixels=5).",
            "proValue": "These are the exact stretches pros use (DS9 zscale + asinh) — gives a familiar, scientifically honest rendering rather than a naive min/max that washes out everything.",
            "accuracy": "zscale operates on TRUE values (post BZERO/BSCALE); reject NaN before sampling; clip display to limits; keep the stretch separate from the underlying values so the cursor still reports raw physical value+BUNIT. Match astropy defaults to reproduce DS9.",
            "effort": "Medium — zscale iterative fit ~60 lines; stretches are one-liners.",
            "source": "DS9 'How it works' https://ds9.si.edu/doc/ref/how.html ; astropy ZScaleInterval https://docs.astropy.org/en/stable/api/astropy.visualization.ZScaleInterval.html ; Lupton et al. 2004 asinh (SDSS)"
          }
        ],
        "summary": "All CORS claims below were verified with live curl using an Origin header (network reality, not docs). BROWSER-DIRECT (Access-Control-Allow-Origin present, no backend needed): SIMBAD TAP + Sesame (ACAO:*), VizieR TAP (ACAO:* — this is your CORS-open route to Gaia DR3 via mirror I/355/gaiadr3), IMCCE Skybot conesearch (ACAO:*, returns asteroid IDs at RA/Dec/epoch as JSON — the right MPChecker replacement), MPC data API (ACAO echoes Origin, JSON, orbits only), and crucially CDS hips2fits (ACAO:*, returns real application/fits). CORS-BLOCKED, require a proxy/serverless function (they 200 but send NO ACAO on GET and OPTIONS): Gaia ESA TAP (gea.esac), Astro Data Lab TAP (datalab.noirlab), NED (ObjectLookup + TAP), and IRSA/ZTF TAP. TNS is the hardest: it needs a custom User-Agent 'tns_marker{...}' header that browsers forbid setting AND a secret key — fundamentally backend-only. FITS recommendation: hand-roll a DataView parse for hips2fits cutouts (fetch->arrayBuffer; 2880-byte header blocks until END card; read BITPIX/NAXIS1/2/BZERO/BSCALE; big-endian DataView with littleEndian=false; BITPIX -32=>getFloat32, 16=>getInt16, 8=>getUint8; true value=BZERO+BSCALE*raw; handle NaN/BLANK; report raw physical value+BUNIT at cursor). My live test cutout was BITPIX=16, 50x50, WCS in CDELT/CTYPE='RA---TAN' form (no CD matrix, no rotation) — implement the exact gnomonic TAN inverse for sub-arcsec cursor RA/Dec, but branch to handle CD1_1..CD2_2 since other surveys use it. Use a library (astrojs/fitsjs, or JS9 for a full viewer) only if you need tile-compressed images, binary tables, or cubes; for cutouts manual is more accurate and auditable. Stretches: linear, log, asinh (Lupton/SDSS), and DS9 zscale limit-finder matching astropy ZScaleInterval defaults (n_samples=1000, contrast=0.25, krej=2.5, max_iterations=5, max_reject=0.5) — always compute on true values, exclude NaN, clip to [0,1]. CONCRETE LAYOUT RECOMMENDATION: keep all cross-match/enrichment that must stay no-backend on the CORS-open set (SIMBAD/Sesame for IDs+types, VizieR for Gaia DR3 + 2MASS/WISE photometry, Skybot for moving-object ID), use hips2fits for the imaging panel with a manual FITS parser + WCS readout + zscale/asinh stretch toggles, and put a single thin proxy in front of the four CORS-blocked services (Gaia-ESA/NED/IRSA-ZTF) plus TNS (which also needs the secret UA marker) if you need them."
      },
      {
        "items": [
          {
            "title": "Spatial all-sky alert triage (head-locked transient dome) — HIGHEST value/effort",
            "detail": "Render the live ALeRCE/ANTARES alert markers on the inside of the 1:1 celestial sphere you already have, color-coded by ML class, and let the astronomer turn their head to scan the whole transient sky at once. The one daily pro workflow (broker/marshal shift triage: what changed tonight, what needs a spectrum now) where VR ~100 deg FOV plus proprioceptive where-did-I-see-that memory genuinely beats panning a 2D all-sky map. Gaze-dwell or controller-ray to pop classification + light curve + cutout. Add a density/heatmap mode and a new-since-last-refresh pulse. Flagship Pro-VR feature.",
            "proValue": "Replaces cognitively expensive pan-and-scroll of 2D broker UIs (ALeRCE/Fink/ANTARES marshals) with a single head-sweep; spatial memory aids re-finding fields and noticing clustering (kilonova counterpart region, galactic-plane variable pile-up). Directly serves the Rubin-era firehose triage problem the app already targets.",
            "effort": "Low-medium. Markers, sphere, and alert ingest already exist; work is head-locked sweep UX, gaze-dwell selection, class legend, and a since-last-poll diff. No new backend.",
            "accuracy": "Markers at true RA/Dec (frame math/tests exist). Color = broker actual ML class label, not a recolor; show class probability and real/bogus score verbatim. Never imply a position you do not have; show alert age so tonight is honest. Display broker attribution per object.",
            "dataOrApi": "Already integrated: ALeRCE (public/transients/tonight.json) and ANTARES (tonight-antares.json), REST/CORS handled in-app. No new endpoint; optionally subscribe to live ANTARES stream near gaze direction.",
            "source": "https://arxiv.org/abs/2412.12383 ; https://baas.aas.org/pub/2021n1i541p11/release/1 ; app README"
          },
          {
            "title": "Immersive 3D local stellar volume — real value ONLY with visible distance uncertainty",
            "detail": "The Gaia/HYG parallax flythrough is genuinely useful for the SOLAR NEIGHBORHOOD (inside the shell where fractional parallax error is small, ~tens to low-hundreds of pc), where stereo depth lets a pro perceive true 3D structure (moving groups, local bubble, co-moving pairs, cluster membership) that collapses on a 2D projection. Validated by Gaia Sky VR and the Exploring and Interrogating Astrophysical Data in VR study. Catch: past the reliable-parallax shell, 1/parallax distances are fiction, so the scene must make that boundary unmissable.",
            "proValue": "Stereoscopic depth measurably improves 3D structure identification and outlier detection in point clouds (peer-reviewed astro-VR); aids membership/kinematics intuition that 2D RA/Dec/parallax scatter plots hide. Encode proper-motion as vectors for tangential-velocity reasoning, as Gaia Sky does.",
            "effort": "Medium. Flythrough exists (109k HYG now; 1.9M Gaia DR3 planned). Added work: per-star uncertainty rendering, reliable-parallax shell boundary cue, proper-motion vector option.",
            "accuracy": "Make-or-break. Use Bailer-Jones geometric/photogeometric distances (already planned), not raw 1/parallax. Render distance uncertainty visibly (radial error-rod length, or opacity/desaturation falloff tied to fractional parallax error) plus a hard you-are-now-extrapolating cue past the shell. Color = real Gaia BP-RP; brightness = real magnitude/distance, not artistic. Floating-origin/double precision so positions do not jitter when flying out.",
            "dataOrApi": "Gaia DR3 + Bailer-Jones EDR3 distances (offline pipeline to binary chunks, designed). HYG today. No CORS issue (build-time).",
            "source": "https://arxiv.org/pdf/2012.10342 ; https://iopscience.iop.org/article/10.3847/1538-3881/abd806 ; https://www.cosmos.esa.int/web/gaia/gaiadr2_gaiasky ; app docs/04-star-catalog-pipeline.md"
          },
          {
            "title": "Light-curve / spectra panels pulled up next to the object (data-at-the-source)",
            "detail": "On gaze/select of a star or transient, spawn a floating panel anchored beside it: real light curve (ALeRCE/ANTARES photometry), spectrum if available, SIMBAD readout, hips2fits cutout, co-located in 3D with the sky position so context is never lost. Optionally a wall of several panels for side-by-side candidate comparison during triage. High-accuracy low-risk workhorse: quantitative 2D plots placed in 3D, not invented 3D.",
            "proValue": "Keeps the diagnostic plot (what pros actually decide on) in the same visual frame as the sky position, eliminating the context-switch between a sky map and a separate light-curve window. Panel-wall supports rapid candidate vetting (the core follow-up-or-not decision).",
            "effort": "Low. Already ship @pmndrs/uikit (the exact VR-panel library), SIMBAD/TAP, hips2fits cutouts, broker light curves. Mostly wiring existing data into anchored panels.",
            "accuracy": "Plots are real photometry with real error bars and band labels; show MJD/filter, magnitude vs flux units explicitly. Cutouts labeled with survey + epoch. Do not smooth/interpolate light curves to hide sampling — show actual points.",
            "dataOrApi": "ALeRCE/ANTARES light curves; SIMBAD cone+TAP; hips2fits cutouts; Sesame names — all CORS-open and in-app.",
            "source": "App README/architecture; @pmndrs/uikit in tech stack"
          },
          {
            "title": "Sonification of light curves / spectra (accessibility + fast triage)",
            "detail": "Pipe the selected object light curve (and optionally spectrum) through pitch-mapping so the astronomer hears variability while looking at the sky — periodic vs transient vs flat is often faster by ear, and it makes the tool usable by blind/low-vision researchers. Pairs with the head-locked alert dome: hear the candidate as you gaze at it.",
            "proValue": "Peer-reviewed work (astronify MNRAS study) shows sonification can match visual signal detection in evenly-sampled light curves and is used in real classification (Zooniverse). Major accessibility win for a Pro tool and a genuine speed aid for periodicity/transient discrimination during triage.",
            "effort": "Low. Web Audio API in-browser; map magnitude to pitch over time. No backend. Can mirror astronify/strauss mappings.",
            "accuracy": "Document the mapping (which quantity to pitch/tempo/pan, linear vs log) on screen; keep it perceptually uniform and reversible so two researchers hear the same data the same way. No musical embellishment not in the data.",
            "dataOrApi": "Reuse the broker light-curve arrays; mappings per astronify (STScI) / strauss.",
            "source": "https://academic.oup.com/mnras/article/516/4/5674/6698731 ; https://arxiv.org/pdf/2209.04465 ; https://baas.aas.org/pub/2022n6i242p16/release/1"
          },
          {
            "title": "Multi-wavelength HiPS layer comparison in 3D — useful but mostly a 2D task",
            "detail": "Cross-fade / blink / side-by-side the survey layers you already ladder (DSS2, Pan-STARRS, unWISE/IR, Rubin, etc.) on the sphere. Honest grade: fundamentally a registered 2D blink-comparator problem; VR adds little over Aladin overlay except bigger FOV. Earns its place only when tied to the spatial alert dome (show me the IR counterpart at this alert position) or when a swipe-able depth slider between layers genuinely helps. Do not headline as a VR feature.",
            "proValue": "Multi-wavelength counterpart checks are real pro work (is this transient on a galaxy in unWISE? a star in Pan-STARRS?). In VR the value is convenience-at-the-gaze-position, not a new perceptual capability.",
            "effort": "Low-medium. Survey ladder + auto-survey-by-view already implemented; add blink/opacity/region-locked compare. Watch texture-pool/draw-call budget on Quest (perf docs flag this).",
            "accuracy": "All layers reprojected to the SAME HEALPix frame (HiPS guarantees this) so pixels co-register; label each layer survey, band, and obs_copyright. Never blend in a way that implies a measurement (no fake color from misaligned bands).",
            "dataOrApi": "CDS HiPS (DSS2, Pan-STARRS, DECaPS, unWISE, Rubin First Look, HST/JWST) — already streamed and composited.",
            "source": "App README; https://arxiv.org/abs/2412.12383"
          },
          {
            "title": "Comfort, locomotion and scale that keep positions scientifically truthful",
            "detail": "Two locomotion modes chosen for accuracy not just comfort: (1) on the celestial sphere (alert dome / sky view) the user is at the CENTER and only rotates the head — no translation, positions exact, vection sickness zero; (2) in the stellar flythrough use teleport/dash + a scale-grip (grab to rescale the volume) rather than continuous joystick flight, since teleport avoids vection and the literature shows it does not hurt spatial orientation. Always show a physical scale bar (pc/ly) and current units; snap to reference scales (1, 10, 100 pc).",
            "proValue": "Keeps sessions long and nausea-free (pros will not adopt a tool that makes them sick in 5 min) AND prevents the silent accuracy bug where comfort hacks (arbitrarily shrinking the universe) distort perceived distances. Scale bar + reference shells make depth quantitative.",
            "effort": "Low-medium. Teleport/dash and head-only sphere mode are standard WebXR; scale-grip + persistent scale legend are modest UI.",
            "accuracy": "Maintain a single documented world-to-parsec scale at all times and surface it; floating-origin/double precision so rescaling/translation introduces no positional drift. Teleport (no optical flow) avoids vection without distorting geometry; no FOV-vignette tricks that crop real sky.",
            "dataOrApi": "None (rendering/UX). Floating-origin already in the Gaia pipeline plan.",
            "source": "https://arxiv.org/html/2312.09737v1 ; https://www.frontiersin.org/journals/virtual-reality/articles/10.3389/frvir.2021.730792/full ; app docs/06-performance.md"
          },
          {
            "title": "Collaborative multi-user inspection — high value, but conflicts with $0/no-backend",
            "detail": "Shared session where two+ astronomers see the same sky/volume with avatars, voice, pointing, and shared annotations — proven valuable (Purdue Collaborative Astronomy VR, StarGateVR, 1990s AstroVR collaboratory) for come-look-at-THIS-candidate and remote co-vetting of transients. But it needs a realtime sync server (presence, position, annotations), which breaks the app explicit $0/static/no-stateful-backend constraint. Treat as a later opt-in tier, not v1.",
            "proValue": "Real for collaborative triage and teaching/observing-run discussion; spatial pointing (that one, two o'clock high) is faster than reading coordinates over a call. Validated across multiple astro-VR collaboration projects.",
            "effort": "High. Needs realtime presence/sync (WebRTC or small WebSocket relay), conflict-free annotation state, voice — outside today static-hosting model, adding ops and cost.",
            "accuracy": "Everyone shares one authoritative coordinate frame and one scale; annotations store RA/Dec (+ distance + uncertainty), not screen-space, so a pointed-at object is unambiguous across users. Sync latency must not let two users disagree about which object is selected.",
            "dataOrApi": "New infra required: WebRTC mesh or lightweight relay (e.g. Cloudflare Durable Object/WebSocket) — outside the current $0 static + thin-proxy model.",
            "source": "https://baas.aas.org/pub/2021n1i541p11/release/1 ; https://www.immsci.com/home/stargatevr/ ; https://ui.adsabs.harvard.edu/abs/1995ASPC...77...99V ; app docs/00-vision.md non-goals"
          }
        ],
        "summary": "Of the eight candidate features, four carry real scientific value for pros given this app's exact assets (HiPS + Gaia/HYG parallax stars + live ALeRCE/ANTARES alerts + WebXR), three are useful-but-conditional, and one (collaborative multi-user) is high-value but high-effort and conflicts with the $0/no-backend constraint. Highest value-per-effort is SPATIAL ALL-SKY ALERT TRIAGE: head-locked on a 1:1 celestial sphere, the one task where VR wide FOV + proprioception genuinely beat a 2D monitor for a daily pro workflow (broker shift triage), and the app already has the alert stream and the sphere. The 3D local-stellar-volume flythrough is real value ONLY if distance UNCERTAINTY is visible (Bailer-Jones fractional-parallax error must be rendered, or 3D positions are quietly fictional past a few hundred pc). Light-curve/spectra panels-next-to-object and sonification are cheap, high-accuracy wins exploiting the @pmndrs/uikit panels already shipped. Multi-wavelength HiPS comparison in 3D is mostly a 2D blink-comparator task: a nice, not a need, unless tied to spatially-registered cutouts at the gaze position. Recurring failure mode in the literature (ASA review, iDaVIE): VR that LOOKS quantitative but isn't; every position, color, and size must map to a documented physical quantity with visible uncertainty or it is a demo. Layout recommendation: default Pro-VR entry should be the head-locked alert dome (not the flythrough); make the flythrough a deliberate depart-Earth mode with an always-visible distance-uncertainty legend and a hard you-are-now-extrapolating cue past the reliable-parallax shell."
      }
    ],
    "synth": null
  }
}